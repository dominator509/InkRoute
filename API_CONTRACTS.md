# API Contracts

## Current status

API contracts are documented and multiple public/dashboard boundaries now have tenant-scoped DB persistence, audit metadata, fail-closed production guards, or local-runtime fallback behavior. Provider execution, live replay proof, full integration tests, and some production workflows remain evidence-gated. Phase 2 expanded the Prisma domain model and validators that future API handlers should use. Phase 3 public pages still read static demo config. All future API endpoints must validate inputs with `@inkroute/validators`, enforce auth where required, scope tenant-owned data, and return a predictable response envelope.

## Response envelope

Recommended success shape:

```ts
type ApiSuccess<T> = {
  ok: true;
  data: T;
};
```

Recommended error shape:

```ts
type ApiError = {
  ok: false;
  error: {
    code: string;
    message: string;
    requestId?: string;
  };
};
```

## Required handler pipeline

All route handlers must:

1. Parse input with Zod.
2. Authenticate if private.
3. Resolve tenant by domain, tenant slug, or authenticated membership.
4. Verify tenant membership for dashboard/mobile endpoints.
5. Check role/custom-role permission.
6. Execute tenant-scoped Prisma query.
7. Return response envelope.
8. Write audit log for sensitive state changes.
9. Redact PII/medical data from logs and errors.
10. Derive public-route client IP/rate-limit keys from allowlisted IP headers only; do not materialize full request headers into helper inputs.
11. Do not trust caller-controlled `x-client-ip` for public or dashboard rate-limit identity; prefer proxy-set headers such as `x-forwarded-for`, `x-real-ip`, `cf-connecting-ip`, or `forwarded`.
12. Local webhook persistence may record signature presence only; never persist raw provider signature headers.
13. Webhook responses should expose explicit non-echo proof for provider signatures instead of returning raw signature header values.
14. SMS webhook responses must not echo raw inbound message bodies; return presence/intent metadata and redacted reconciliation summaries only.

## Public endpoints planned

| Method | Route | Purpose | Validator | Auth | Status |
| --- | --- | --- | --- | --- | --- |
| `GET` | `api/public/:tenantSlug/portfolio` | Public portfolio items | `publicReadQuerySchema` bounded `limit` | No | DB-oriented tenant-scoped read with validated bounded query limit; DB-backed rows without public image metadata are omitted instead of falling back to demo placeholder assets. Non-production demo fallback remains available; production disables local fallback. |
| `GET` | `api/public/:tenantSlug/travel` | Public travel schedule/city availability | `publicReadQuerySchema` bounded `limit` | No | DB-oriented tenant-scoped read with validated bounded query limit and non-production demo fallback; production disables local fallback. |
| `POST` | `/api/public/:tenantSlug/booking-requests` | Create booking request | `bookingRequestInputSchema` | No, DB path requires bot proof + queue handoff readiness | Tenant-scoped validation, local + DB persistence path, anti-bot proof enforcement for DB writes with response-level proof flags that do not echo body hashes, encryption-policy/rotation metadata, signed reference-upload handoff contract projected without raw booking IDs or signed URLs, sanitized workflow/provider-failure/audit payload projection without raw tenant, booking, workflow, or local consumer IDs, ID-free audit proof (`auditLogged`) inside the persistence result, state-event metadata-free response projection without audit ID, state-event ID, actor-user ID, or internal persistence ID echoes, and public booking receipt allowlisting with no raw contact, idea-summary, or medical-note response echoes. |
| `POST` | `api/public/:tenantSlug/waitlists` | City waitlist signup | `waitlistSignupInputSchema` | No, rate-limited | DB-oriented Client/MessageThread/Message/Notification/NotificationDelivery/NotificationProviderHandoff/IdempotencyKey/AuditLog intent persistence with hashed destination metadata, SHA-256 fingerprinted provider-handoff idempotency correlation outside the IdempotencyKey row, and ID-free audit/handoff metadata proof, request-hash idempotency conflict denial, completed replay receipts without duplicate side effects, ID-free IdempotencyKey receipt proof with `internalPersistenceIdsStored=false`, safe receipt response projection without raw or masked contact, destination hash, idempotency key, raw idempotency-result internals, internal row IDs, waitlist body, or local redacted-payload echoes, and non-production local fallback; production disables local fallback and live provider sends remain gated. |
| `GET` | `api/public/:tenantSlug/seo/cities/:citySlug` | City SEO page data | `seoCityPageInputSchema` for admin writes | No | DB-oriented published city-page read with non-production demo fallback; production disables local fallback. |
| `GET` | `api/public/:tenantSlug/seo/styles/:styleSlug` | Style SEO page data | `seoStylePageInputSchema` for admin writes | No | DB-oriented published style-page read with non-production demo fallback; production disables local fallback. |
| `GET` | `api/public/:tenantSlug/reviews` | Approved (evidence-gated) public testimonials | `publicReadQuerySchema` bounded `limit` | No | DB-oriented tenant-scoped approved-review read with validated bounded query limit and non-production demo fallback; production disables local fallback. |
| `GET` | `api/public/:tenantSlug/faq` | Public FAQ content bundle | `publicReadQuerySchema` bounded `limit` | No | Tenant-safe public bundle route with validated bounded query limit; durable FAQ CMS persistence remains under SEO/public-content gaps. |
| `GET` | `/api/public/:tenantSlug/notification-previews` | Static notification/template delivery-plan previews | Static Phase 9 helper output | No | Scaffolded preview route with safe delivery-plan projection; no raw contact, raw destination, masked destination, raw consent snapshot, tenant ID, or internal persistence ID echoes (`tenantIdEchoed=false`, `internalPersistenceIdsEchoed=false`); no send/queue |
| `POST` | `/api/public/:tenantSlug/messages` | Public client message/contact thread draft | `publicMessageInputSchema` | No, DB path requires existing booking/client context | DB-first public message persistence for tenant-scoped booking replies; validates public subject/body/booking context, writes MessageThread/Message/Notification/Delivery/Handoff/IdempotencyKey/AuditLog rows with SHA-256 hashed destination metadata and SHA-256 fingerprinted provider-handoff idempotency correlation outside the IdempotencyKey row, stores ID-free provider-handoff and audit metadata proof with `internalPersistenceIdsStored=false`, completes IdempotencyKey result proof with `internalPersistenceIdsStored=false`, short-circuits completed idempotency replays before duplicate side effects, returns a safe receipt/draft projection without thread/message/notification/delivery/handoff/audit/idempotency-key/booking-request ID echoes, no raw contact, raw message body, body preview, destination-hash, or idempotency-key response echoes, and no client profile-name DB projection when booking context exists, otherwise local fallback stays non-production only. Provider worker execution remains evidence-gated. |
| `POST` | `/api/public/:tenantSlug/contact` | Public contact form intake | `publicContactInputSchema` | No, rate-limited | DB-first contact intake: validates public name/email/subject/message, resolves tenant scope, writes/short-circuits IdempotencyKey before Client/message side effects, upserts Client by email, writes MessageThread/Message/Notification/Delivery/Handoff/AuditLog rows with hashed destination metadata, SHA-256 fingerprinted provider-handoff idempotency correlation outside the IdempotencyKey row, and ID-free provider-handoff/audit metadata proof plus generated hash-fingerprint idempotency keys, completes IdempotencyKey result proof with `internalPersistenceIdsStored=false`, returns a safe contact receipt with tenant slug/scope proof (`tenantIdEchoed=false`) but without raw tenant ID, raw contact/message, redacted submission, destination hash, idempotency key, or internal persistence ID echoes, and keeps local runtime fallback non-production only. Provider delivery, seeded DB isolation, browser/API E2E, and CI evidence remain gated. |
| `POST` | `/api/public/:tenantSlug/analytics` | Public SEO analytics attribution event | `AnalyticsEventName` allowlist | No, tenant-scoped and redacted | DB-first public analytics ingestion: resolves tenant scope, normalizes/redacts UTM/portfolio/booking attribution, persists AnalyticsEvent/Campaign rows with idempotency key, returns idempotency proof with `keyEchoed=false` instead of raw keys, and disables production local preview fallback. SearchConsoleImportedRow/SearchConsoleOperationRun schema and migration coverage are present for import durability, while credentialed Search Console jobs, click-through proof, booking attribution integration, and CI evidence remain gated. |

Public SEO analytics ingestion responses now suppress tenant, booking, portfolio, raw attribution, and raw idempotency identifiers (`tenantIdEchoed=false`, `bookingRequestIdEchoed=false`, `portfolioItemIdEchoed=false`, `rawAttributionIdsEchoed=false`, `rawIdempotencyKeyEchoed=false`) on database and local-preview acceptance paths while preserving event name, public URL/UTM/city/style context, persistence booleans, and Search Console/provider blockers; Search Console credentials/jobs, click-through proof, booking attribution integration, dashboard reporting proof, and CI remain gated under `GAP-074`/`GAP-078`.
| `POST` | `/api/public/:tenantSlug/preferences` | Public notification preference mutation | Preference center contract | No, signed-token proof required before launch | DB-first preference mutation: resolves tenant scope, persists hash-only NotificationChannelPreference/NotificationSuppression/IdempotencyKey/AuditLog rows with ID-free audit metadata proof, completes ID-free IdempotencyKey result proof with `internalPersistenceIdsStored=false`, does not select client email in DB response projections, returns safe preference-plan and persistence receipt projections with raw token, token hash, idempotency key, write payload, client ID, preference ID, suppression ID, idempotency-key ID, audit ID, and internal persistence ID echoes disabled, and keeps a non-production local-contract fallback with the same explicit no-echo response projection. Token crypto, legal copy, List-Unsubscribe provider proof, integration tests, and CI evidence remain gated. |
| `POST` | `/api/public/:tenantSlug/unsubscribe` | One-click email unsubscribe | Preference center contract | No, token hash required for production proof | DB-first one-click unsubscribe: persists hash-only email opt-out, suppression, idempotency, and audit rows with ID-free audit metadata proof when DB tenant scope is available, completes ID-free IdempotencyKey result proof with `internalPersistenceIdsStored=false`, never stores raw tokens, does not select client email in DB response projections, returns safe unsubscribe-plan and persistence receipt projections with raw token, token hash, idempotency key, write payload, client ID, preference ID, suppression ID, idempotency-key ID, audit ID, and internal persistence ID echoes disabled, and keeps a non-production local-contract fallback with the same explicit no-echo response projection. Provider List-Unsubscribe proof remains gated. |

Public preference GET responses expose the preference runtime/readiness and required repository method contract through safe plan summaries only; bundled demo mutation plans are projected with `rawContractPlansEchoed=false`, `rawTokenEchoed=false`, `tokenHashEchoed=false`, `rawEmailEchoed=false`, `rawPhoneEchoed=false`, `idempotencyKeyEchoed=false`, and `writePayloadsEchoed=false` instead of returning raw demo tokens, contact values, idempotency keys, token hashes, or write payloads.

Package preference mutation plans keep token/contact hashes on operational preference, suppression, and token writes, but `NotificationAuditLog` plan payloads now store presence booleans plus SHA-256 proof hashes and `rawEmailHashStored=false`, `rawPhoneHashStored=false`, `rawTokenHashStored=false`, and `rawIdempotencyKeyStored=false` instead of duplicating raw token/contact/idempotency hash material.

Package notification persistence plans keep raw thread/message/notification/delivery/client/actor identifiers, destination hashes, and idempotency keys on operational write models where required, but `NotificationAuditLog` plan payloads now store SHA-256 proof hashes and `rawThreadIdStored=false`, `rawMessageIdStored=false`, `rawNotificationIdStored=false`, `rawDeliveryIdStored=false`, `rawClientIdStored=false`, `rawActorIdStored=false`, `rawDestinationHashStored=false`, and `rawIdempotencyKeyStored=false` instead of duplicating raw identifiers or hash material.
| `POST` | `/api/public/:tenantSlug/error-reports` | Public fallback client error-report draft | `errorReportInputSchema` | No, rate-limited and bot-protected | DB-backed redacted ErrorReport + AbuseEvent + AuditLog ingest with safe receipt projection and no error-report/audit/abuse-event/internal persistence ID echoes; production local fallback disabled; provider forwarding remains evidence-gated. |

## Dashboard endpoints planned

| Method | Route | Purpose | Permission | Validator | Status |
| --- | --- | --- | --- | --- | --- |
| `GET` | `api/metrics` | Tenant overview metrics | `analytics:read` | `dashboardMetricsQuerySchema` optional `tenantId`; unknown query keys rejected | DB-backed aggregate route with dashboard auth/RBAC, tenant-scope enforcement, no-store responses, AuditLog write, safe read projection without tenant ID, audit ID, or internal persistence ID echoes across database, local-fallback, production fail-closed, and database-unavailable branches while preserving tenant-match proof, and non-production demo fallback; production fallback disabled. |
| `GET` | `/api/bookings` | Booking inbox | `booking:read` | `dashboardListQuerySchema` optional `tenantId`, bounded `limit` | DB-backed route exists: tenant-scoped BookingRequest list read, redacted dashboard projection, no-store response, AuditLog write, safe read projection without BookingRequest, tenant, portfolio attribution, assignee, audit, or internal persistence ID echoes, and production local fallback fail-closed. Seeded DB/RBAC/redaction tests remain evidence-gated. |
| `GET` | `/api/bookings/:bookingId` | Booking detail with timeline | `booking:read` | Path `bookingId`; `dashboardTenantQuerySchema` optional `tenantId` | DB-backed route exists: tenant-scoped BookingRequest detail read with latest state events, redacted dashboard projection, no-store response, AuditLog write, safe read projection without audit ID or internal persistence ID echoes, and production local fallback fail-closed. Seeded DB/RBAC/redaction tests remain evidence-gated. |
| `PATCH` | `/api/bookings/:bookingId/state` | Accept/decline/reschedule state changes | `booking:write` | `bookingStatusUpdateSchema` plus action/idempotency metadata | DB-backed route exists: validates supported lifecycle action, enforces tenant scope, replays completed IdempotencyKey results without duplicate writes, persists BookingRequest status + IdempotencyKey + BookingStateEvent + AuditLog in one transaction, stores ID-free replay proof (`bookingStateEventPersisted`/`auditLogged` plus `internalPersistenceIdsStored=false`) and ID-free event/audit metadata proof with raw idempotency-key storage denial instead of booking/event/audit/idempotency IDs, emits dashboard mutation plan evidence through a safe receipt projection without tenant ID, booking/request ID, audit ID, event ID, idempotency-key ID, raw idempotency result, or internal persistence ID echoes across success/replay, production/local fallback, and database-unavailable branches while preserving tenant-match proof, and disables production local fallback. Provider rollback and seeded integration tests remain evidence-gated. |

Booking lifecycle invalid-transition responses return a safe transition-plan projection with `rawTransitionEchoed=false`, `rawWritePayloadsEchoed=false`, `rawTenantIdEchoed=false`, `rawBookingRequestIdEchoed=false`, `rawActorIdEchoed=false`, `rawIdempotencyKeyEchoed=false`, and `internalPersistenceIdsEchoed=false` instead of echoing raw transition internals or write payloads.

Booking lifecycle and appointment mutation success responses return safe dashboard mutation-plan projections with `rawDashboardMutationPlanEchoed=false`, `rawTenantIdEchoed=false`, `rawActorIdEchoed=false`, `rawBookingRequestIdEchoed=false`, and `rawIdempotencyKeyEchoed=false` instead of echoing raw `buildDashboardMutationPlan` output containing tenant scope or idempotency keys.
| `POST` | `api/appointments` | Create appointment from booking | `booking:write` | `appointmentInputSchema` | DB-backed route added: validates booking/artist/client/travel/studio tenant scope, claims/replays IdempotencyKey results, persists Appointment + BookingStateEvent + AuditLog + optional local Deposit draft/PaymentAuditLog + local NotificationJob handoff intent in one transaction, stores ID-free replay proof (`appointmentPersisted`/`bookingStateEventPersisted`/`auditLogged`/handoff flags plus `internalPersistenceIdsStored=false`) plus ID-free lifecycle/deposit/payment-audit metadata proof with raw idempotency-key storage denial, reloads replay by validated booking/artist/client/time fields, advances scheduling state through the booking transition plan, returns deferred Stripe checkout/calendar/provider-notification lifecycle intents, and emits safe receipt projections without tenant ID, appointment/artist/client/booking IDs, audit IDs, idempotency-key IDs, or internal persistence IDs on success/replay, production/local fallback, and database-unavailable responses while preserving tenant-match proof. Production local fallback fails closed; provider execution and tenant-isolated integration tests remain evidence-gated. |

Appointment invalid-transition responses return a safe transition-plan projection with `rawTransitionEchoed=false`, `rawWritePayloadsEchoed=false`, `rawTenantIdEchoed=false`, `rawBookingRequestIdEchoed=false`, `rawActorIdEchoed=false`, `rawIdempotencyKeyEchoed=false`, and `internalPersistenceIdsEchoed=false` instead of echoing raw transition internals or write payloads.

Appointment success responses return safe appointment, booking, state-event, deposit-draft, and notification-job receipts with appointment/booking/event/deposit/job/internal IDs and notification-job payloads suppressed (`appointmentIdEchoed=false`, `bookingRequestIdEchoed=false`, `eventIdEchoed=false`, `depositDraftIdEchoed=false`, `notificationJobIdEchoed=false`, `rawNotificationJobPayloadEchoed=false`, and `internalPersistenceIdsEchoed=false`) instead of echoing raw selected persistence rows.
Appointment duplicate-conflict responses now suppress the existing Appointment row ID (`duplicateAppointmentIdEchoed=false`) while preserving the `APPOINTMENT_ALREADY_EXISTS` conflict code; tenant-isolated duplicate route tests remain evidence-gated under `GAP-038`.
| `GET` | `api/seo` | List dashboard SEO city/style pages and redirects | `seo:read` | Optional `tenantId`, bounded `limit` | DB-backed route exists: enforces tenant/RBAC scope, reads city/style/redirect records with no-store headers and AuditLog write, and returns a no-echo dashboard SEO read projection without tenant, SeoCityPage, SeoStylePage, SeoRedirect, featured PortfolioItem, audit, or internal persistence identifiers (`tenantIdEchoed=false`, `seoPageIdsEchoed=false`, `redirectIdsEchoed=false`, `portfolioItemIdsEchoed=false`, `auditIdEchoed=false`, `internalPersistenceIdsEchoed=false`). Non-production local fallback strips related portfolio IDs and exposes only `relatedPortfolioLinked`; production local fallback fails closed. Seeded tenant/RBAC/redaction tests, dashboard/browser SEO read smoke, Search Console provider proof, and CI evidence remain gated under `GAP-037`/`GAP-071`/`GAP-076`. |
| `POST/PATCH` | `api/seo` | Create/update/publish/archive SEO city/style pages and redirects | `seo:write` | `buildSeoPublicationMutationPlan` plus idempotency/revalidation/association metadata | DB-backed route exists: enforces tenant/RBAC scope, runs the package publication planner before commits, upserts SeoCityPage/SeoStylePage/SeoRedirect records, persists IdempotencyKey + AuditLog + SeoPublicationRevalidationJob + SeoPublicationAssociation rows in one transaction, stores ID-free idempotency replay and claim/job/audit metadata proof flags with `internalPersistenceIdsStored=false` plus raw idempotency-key/write-summary/revalidation-tag/related-entity-ID storage denial, returns no-store response-level idempotency/revalidation proof through a safe plan projection without tenant IDs, SEO entity IDs, actor IDs, audit IDs, idempotency keys/IDs, write summaries, raw route payloads, internal persistence IDs, or raw revalidation paths/tags, and keeps local fallback dry-run only. Prisma migration/client generation, seeded tenant-isolated integration tests, dashboard browser flows, CI evidence, and secret-safe artifacts remain gated under `GAP-071`. |
| `GET` | `/api/clients` | Client list | `client:read` | `dashboardListQuerySchema` optional `tenantId`, bounded `limit` | DB-backed route exists: tenant-scoped Client list read with payment summary, no-store safe read projection without Client, tenant, related booking, audit, or internal persistence ID echoes, AuditLog write, and encrypted medical/private note values replaced by redacted presence indicators before response projection. Production local fallback fails closed; seeded DB/RBAC/redaction tests remain evidence-gated. |
| `GET` | `/api/clients/:clientId` | Client profile/timeline | `client:read` | Path `clientId`; `dashboardTenantQuerySchema` optional `tenantId` | DB-backed route exists: tenant-scoped Client detail read with recent bookings/payment summary, no-store safe read projection without audit ID echoes, AuditLog write, and encrypted medical/private note values replaced by redacted presence indicators before response projection. Production local fallback fails closed; seeded DB/RBAC/redaction tests remain evidence-gated. |
| `POST` | `/api/clients` | Create client profile | `client:write` | `clientInputSchema` | DB-backed route added: validates client payload, normalizes tenant-scoped email uniqueness, claims/replays IdempotencyKey results, persists Client + AuditLog in one transaction, stores ID-free replay and AuditLog metadata proof (`clientPersisted`/`auditLogged` plus `internalPersistenceIdsStored=false`) with raw idempotency-key storage denial, reloads replay by tenant/email, returns no-store safe receipt projection without tenant ID, Client ID, Tenant row ID, audit ID, idempotency-key ID, raw idempotency key, duplicate-client ID, encrypted medical notes, private notes, or internal persistence ID echoes across DB success/replay, duplicate, production/local fallback, unique-conflict, and database-unavailable branches, and disables production local fallback. Tenant-isolated mutation/RBAC/redaction tests remain evidence-gated. |

Client creation receipts now return an allowlisted client payload with `clientIdEchoed=false`, `tenantRecordIdEchoed=false`, and `tenantIdEchoed=false` instead of echoing the raw selected `Client` or tenant row across DB success/replay, duplicate, production/local fallback, unique-conflict, and database-unavailable branches while preserving tenant-match proof; tenant-isolated mutation, RBAC, redaction, and CI evidence remain gated under `GAP-038`/`GAP-040`.
| `PATCH` | `/api/clients/:clientId` | Update private client note metadata | `client:write` | `clientPrivateNoteInputSchema` plus `dashboardTenantQuerySchema` optional `tenantId` | DB-backed private-note route exists: validates tenant/RBAC scope, claims/replays IdempotencyKey results with a hash-only note fingerprint and ID-free stored/replayed proof (`auditLogged` plus `internalPersistenceIdsStored=false`), persists ClientProfile + AuditLog in one transaction, stores ID-free AuditLog metadata proof with raw idempotency-key storage denial, returns safe receipt projections without tenant ID, Client ID, audit ID, idempotency-key ID, raw idempotency key, raw note, or internal persistence ID echoes across DB success/replay, idempotency conflict, not-found, production/local fallback, and database-unavailable branches while preserving tenant-match proof, and disables production local fallback. Export/delete/retention/legal workflow tests remain evidence-gated under privacy gaps. |
| `GET` | `/api/portfolio` | Portfolio item list | `portfolio:read` | `dashboardTenantQuerySchema` optional `tenantId`, bounded `limit` | DB-backed route exists: tenant-scoped PortfolioItem list read with AuditLog write, no-store safe read projection without PortfolioItem, tenant, attributed booking, PortfolioImage, FileAsset, audit, or internal persistence ID echoes, public/private image URL decision metadata, direct nested storage identifiers (`objectKey`, `bucket`, checksum, signed URL expiry) not selected from the database, and metadata keys redacted before response. Production local fallback fails closed; storage provider and derivative proof remain evidence-gated. |
| `GET` | `/api/portfolio/:portfolioId` | Portfolio item detail | `portfolio:read` | Path `portfolioId`; `dashboardTenantQuerySchema` optional `tenantId` | DB-backed route exists: tenant-scoped PortfolioItem detail read with attributed booking names redacted, AuditLog write, no-store response, public/private image URL decision metadata, direct nested storage identifiers not selected from the database, metadata keys redacted before response, and safe read projection without audit ID or internal persistence ID echoes. Production local fallback fails closed; storage provider and derivative proof remain evidence-gated. |
| `POST` | `/api/portfolio` | Create portfolio item | `portfolio:write` | `portfolioItemInputSchema` | DB-backed metadata route added: validates artist/style tenant scope, enforces tenant slug uniqueness, claims/replays IdempotencyKey results, persists PortfolioItem + style links + primary PortfolioImage URL metadata + AuditLog in one transaction, stores ID-free replay and AuditLog metadata proof (`portfolioItemPersisted`/`auditLogged` plus `internalPersistenceIdsStored=false`) with artist match proof and raw idempotency-key storage denial, reloads replay by tenant/slug, returns a safe receipt projection without tenant ID, PortfolioItem ID, artist ID, raw PortfolioImage rows, audit ID, idempotency-key ID, raw idempotency key, duplicate portfolio item ID, storage object key, signed URL, or internal persistence ID echoes across DB success/replay, production/local fallback, artist-not-found, style-not-found, duplicate, and database-unavailable branches, and disables production local fallback. Signed upload/object-storage/image-processing handoff and tenant-isolated mutation tests remain evidence-gated. |

Portfolio metadata creation receipts now suppress PortfolioItem, tenant, artist, duplicate PortfolioItem, and raw PortfolioImage row echoes (`portfolioItemIdEchoed=false`, `tenantIdEchoed=false`, `artistIdEchoed=false`, `duplicatePortfolioItemIdEchoed=false`, `rawPortfolioImagesEchoed=false`) across DB success/replay, production/local fallback, artist-not-found, style-not-found, duplicate, and database-unavailable branches while preserving tenant-match proof, tenant-scoped persistence, style links, primary image URL metadata, and AuditLog metadata internally; signed upload, object storage, image processing, and tenant-isolated mutation evidence remain gated under `GAP-038`/`GAP-077`.
| `POST` | `api/portfolio/:id/images` | Attach portfolio image metadata | `portfolio:write` | `portfolioImageInputSchema` | DB-backed metadata route added at `/api/portfolio/[portfolioId]/images`: validates path/body match, verifies PortfolioItem/FileAsset tenant scope, claims/replays IdempotencyKey results, persists PortfolioImage + AuditLog, stores ID-free replay and AuditLog metadata proof (`portfolioImagePersisted`/`auditLogged`/provider blockers plus `internalPersistenceIdsStored=false`) with portfolio/file match proof and raw idempotency-key storage denial, reloads replay by tenant/portfolio/input metadata, returns a safe receipt projection without tenant, PortfolioImage, PortfolioItem, FileAsset, AuditLog, idempotency-key, image URL, raw idempotency-key, raw image URL, or internal persistence ID echoes across DB success/replay, production/local fallback, portfolio-not-found, file-asset-not-found, and database-unavailable branches, and disables production local fallback. Signed upload, malware scan, derivatives, EXIF stripping, CDN proof, and performance evidence remain gated. |
| `POST` | `api/files/signed-upload` | Create signed upload request | Permission varies by file kind | `fileAssetInputSchema` boundary | DB-backed signed-upload intent route added: validates file metadata, maps permission by file kind, claims/replays IdempotencyKey results, persists FileAsset + SignedUrlGrant + AuditLog rows, stores ID-free replay and AuditLog metadata proof (`fileAssetPersisted`/`signedUrlGrantPersisted`/`auditLogged` plus provider blocker flags and `internalPersistenceIdsStored=false`) with raw idempotency-key storage denial, reloads replay by tenant/bucket/object key/kind, returns no-store safe receipt responses without bucket, object key, signed URL hash, upload URL field, raw plan object, raw idempotency key, or internal persistence ID echoes, `providerUrlMinted: false`, and disables production local fallback. Provider signed URL minting, malware scan, metadata stripping, bucket ACL proof, and cross-tenant provider denial remain gated. |
Signed-upload intent receipts now explicitly prove `tenantIdEchoed=false`, `fileAssetIdEchoed=false`, `signedUrlGrantIdEchoed=false`, `referenceImageIdEchoed=false`, and `auditIdEchoed=false` alongside signed URL/hash/storage-field suppression across DB success/replay, production/local fallback, client-not-found, file-exists, and database-unavailable branches while preserving tenant-match proof; provider-signed URL minting, malware scan execution, bucket ACL proof, and tenant-isolated route execution remain evidence-gated under `GAP-005`/`GAP-038`.
| `GET` | `/api/travel` | Travel schedule list | `travel:read` | `dashboardTenantQuerySchema` optional `tenantId`, bounded `limit` | DB-backed route exists: tenant-scoped TravelSchedule list read with AuditLog write, no-store response, guest-spot URL redaction, nested AvailabilityWindow internal notes redacted before response, and safe read projection without tenant, TravelSchedule, artist, TravelCity, AvailabilityWindow, BookingRequest, Appointment, audit, or internal persistence ID echoes. Production local fallback fails closed; public cache, provider sync, and E2E proof remain evidence-gated. |
| `GET` | `/api/travel/:travelScheduleId` | Travel schedule detail | `travel:read` | Path `travelScheduleId`; `dashboardTenantQuerySchema` optional `tenantId` | DB-backed route exists: tenant-scoped TravelSchedule detail read with attributed booking client names redacted, AuditLog write, no-store response, guest-spot URL redaction, nested AvailabilityWindow internal notes redacted before response, tenant-match proof instead of raw tenant ID echoes, local demo fallback projection without raw travel IDs, and safe read projection without TravelSchedule, tenant, artist, TravelCity, AvailabilityWindow, BookingRequest, Appointment, audit, or internal persistence ID echoes. Production local fallback fails closed; public cache, provider sync, and E2E proof remain evidence-gated. |
| `POST` | `api/travel/cities` | Create managed travel city | `travel:write` | `travelCityInputSchema` | DB-backed route added: validates city payload, enforces tenant slug uniqueness, claims/replays IdempotencyKey results with ID-free replay proof and tenant/slug replay lookup, persists TravelCity + AuditLog in one transaction, stores ID-free AuditLog metadata proof with raw idempotency-key storage denial, returns a no-store safe receipt projection with tenant-match proof and without tenant, TravelCity, audit, idempotency-key, raw idempotency key, duplicate-city, or internal persistence ID echoes across success, replay, duplicate, fail-closed, local-fallback, and database-unavailable branches, and disables production local fallback. Public SEO/cache revalidation and tenant-isolated mutation tests remain evidence-gated. |
Travel city duplicate-slug responses now suppress the existing TravelCity row ID (`duplicateTravelCityIdEchoed=false`) while preserving the `TRAVEL_CITY_SLUG_EXISTS` conflict code; public cache/SEO and seeded tenant-isolated duplicate tests remain evidence-gated under `GAP-038`.
| `POST` | `api/travel/schedules` | Create travel schedule/guest spot | `travel:write` | `travelScheduleInputSchema` | DB-backed route added: validates schedule payload, verifies artist/city/studio tenant scope, claims/replays IdempotencyKey results with ID-free replay proof and validated schedule-field replay lookup, persists TravelSchedule + AuditLog in one transaction, stores ID-free AuditLog metadata proof with artist/city match flags and raw idempotency-key storage denial, queues a local NotificationJob waitlist fanout intent, returns a no-store safe receipt projection with tenant-match proof and without tenant, TravelSchedule, artist, city, studio, audit, notification-job, idempotency-key, raw idempotency key, or internal persistence ID echoes across success, replay, fail-closed, local-fallback, and database-unavailable branches, and disables production local fallback. Public cache/SEO/provider fanout execution and tenant-isolated mutation tests remain evidence-gated. |

Travel city and travel schedule creation success receipts now suppress TravelCity, TravelSchedule, tenant, artist, travel-city, and studio row IDs at the response boundary while preserving tenant-scoped persistence, replay lookups, and AuditLog metadata internally; public cache/SEO, provider fanout, and tenant-isolated mutation evidence remain gated under `GAP-038`.
| `POST` | `api/travel/publish` | Publish/update/unpublish travel stop | `travel:write` | Travel publish mutation plan | DB-first route added: validates publish/update/unpublish/rollback plans, verifies artist tenant scope, upserts TravelCity, persists/updates TravelSchedule, records AuditLog and ID-free IdempotencyKey stored/replayed proof metadata with request-hash conflict denial, raw idempotency-key/request-hash/revalidation-tag storage denial, and revalidation tag counts, returns a no-store safe receipt projection without audit ID, idempotency-key ID, raw idempotency key, travel city ID, travel schedule ID, or internal persistence ID echoes plus a safe plan projection without raw stop payloads, waitlist client IDs, revalidation tags, rollback reasons, actor IDs, or idempotency keys, and keeps repository-required fallback non-production only. Cache revalidation, notification provider queues, sync transports, rollback provider tests, and dashboard-to-public E2E proof remain evidence-gated under `GAP-060`. |
| `POST` | `api/availability` | Create availability window | `travel:write` | `availabilityWindowInputSchema` | DB-backed route added: validates availability payload, verifies artist/city/schedule tenant scope, claims/replays IdempotencyKey results, persists AvailabilityWindow + AuditLog in a serializable transaction, applies a persisted overlap guard, stores ID-free replay proof (`availabilityWindowPersisted`/`auditLogged`/overlap and hold-protection flags plus `internalPersistenceIdsStored=false`) and ID-free AuditLog metadata match proof with raw idempotency-key storage denial, reloads replay by validated tenant/artist/travel/time fields, returns safe success/conflict receipt projections without audit ID, idempotency-key ID, raw idempotency key, conflicting availability-window ID, or internal persistence ID echoes, and disables production local fallback. Concurrent hold race proof and seeded integration tests remain evidence-gated under `GAP-056`. |

Availability creation success receipts now suppress `AvailabilityWindow`, tenant, artist, travel-city, and travel-schedule row IDs at the response boundary while preserving tenant-scoped persistence and audit metadata internally; seeded race-condition and integration proof remain gated under `GAP-038`/`GAP-056`.
| `GET` | `/api/calendar` | Calendar dashboard feed | `calendar:read` | `dashboardTenantQuerySchema` optional `tenantId`, bounded `limit` | DB-backed route exists: tenant-scoped CalendarConnection, CalendarEvent, and AvailabilityWindow reads with AuditLog write, no-store safe read projection without tenant, CalendarConnection, CalendarEvent, AvailabilityWindow, artist, appointment, travel-city, travel-schedule, audit, or internal persistence ID echoes, provider account/external event IDs redacted, raw provider payloads replaced with count/presence-only previews, linked/presence booleans, and availability internal-note redaction. Production local fallback fails closed and non-production fallback uses the same no-echo projection; OAuth sync/provider writes remain evidence-gated. |
| `POST` | `api/calendar/holds` | Create dashboard slot hold | `calendar:write` | Availability hold plan | DB-first route added: validates hold plan, verifies artist tenant scope, claims/replays IdempotencyKey rows with request-hash conflict denial before conflict checks, rejects overlapping open/waitlist/full windows for new holds, persists `AvailabilityWindow` admin holds with `AuditLog`, stores ID-free idempotency replay proof flags (`availabilityWindowPersisted`/`auditLogged` plus `internalPersistenceIdsStored=false`) and ID-free IdempotencyKey/AuditLog metadata proof with raw idempotency-key/request-hash storage denial, returns no-store safe receipt projections without audit ID, idempotency-key ID, raw idempotency key, raw request hash, raw plan payload, raw hold record, conflicting window ID, or availability-window ID echoes, and disables production local fallback. Seeded race-condition, cross-tenant, and repository integration proof remain evidence-gated under `GAP-056`. |
| `GET` | `/api/payments` | Payment list | `payment:read` | `dashboardTenantQuerySchema` optional `tenantId`, bounded `limit` | DB-backed route exists: tenant-scoped Payment list read with AuditLog and PaymentAuditLog writes, no-store response, provider payment/session IDs and receipt URLs replaced by redacted presence indicators, recursive payment metadata redaction, ID-free PaymentAuditLog metadata proof (`auditLogged=true`, `internalPersistenceIdsStored=false`), and safe read projection without Payment, tenant, BookingRequest, Deposit, Refund, audit, or internal persistence ID echoes. Production local fallback fails closed and non-production fallback uses the same no-echo projection; Stripe provider proof remains evidence-gated. |
| `GET` | `/api/payments/:paymentId` | Payment detail | `payment:read` | Path `paymentId`; `dashboardTenantQuerySchema` optional `tenantId` | DB-backed route exists: tenant-scoped Payment detail read with refund summaries, AuditLog and PaymentAuditLog writes, no-store response, provider payment/session IDs and receipt URLs replaced by redacted presence indicators, recursive payment metadata redaction, refund reason redaction, ID-free PaymentAuditLog metadata proof (`auditLogged=true`, `internalPersistenceIdsStored=false`), and safe read projection without AuditLog ID, PaymentAuditLog ID, or internal persistence ID echoes. Production local fallback fails closed; Stripe provider proof remains evidence-gated. |
| `POST` | `api/payments/deposit-session` | Create Stripe deposit session | `payment:write` | `depositInputSchema` | DB-backed deposit draft route added: validates booking/appointment tenant scope, claims/replays IdempotencyKey results, persists Deposit + PaymentAuditLog, stores ID-free replay and PaymentAuditLog metadata proof (`depositPersisted`/`paymentAuditPersisted` plus Stripe/webhook blockers and `internalPersistenceIdsStored=false`) with raw idempotency-key storage denial, reloads replay by validated deposit-scope fields, returns a no-store safe receipt projection with `stripeCheckoutCreated: false`, `providerCheckoutUrlEchoed=false`, and `providerSessionIdEchoed=false`, and no tenant ID, Deposit ID, booking-request ID, appointment ID, audit ID, idempotency-key ID, raw idempotency key, provider checkout URL/session ID, or internal persistence ID echoes across DB success/replay, production/local fallback, related-record-not-found, and database-unavailable branches, and disables production local fallback. Stripe checkout creation, webhook reconciliation, rollback, and sandbox proof remain provider-gated. |

Dashboard deposit-session receipts now suppress tenant, Deposit, booking-request, appointment, provider checkout URL, and provider session identifiers (`tenantIdEchoed=false`, `depositIdEchoed=false`, `bookingRequestIdEchoed=false`, `appointmentIdEchoed=false`, `providerCheckoutUrlEchoed=false`, `providerSessionIdEchoed=false`) across DB success/replay, production/local fallback, related-record-not-found, and database-unavailable branches while preserving tenant-match proof, DB-backed draft persistence, and PaymentAuditLog metadata internally; Stripe checkout creation, webhook reconciliation, rollback, and sandbox proof remain provider-gated under `GAP-038`/`GAP-004`.
| `POST` | `api/refunds` | Create/refund payment record | `payment:write` | `refundInputSchema` | DB-backed local refund-record route added: validates tenant-scoped Payment match, amount/currency/scope constraints, claims/replays IdempotencyKey results with ID-free replay proof and validated refund-scope replay lookup, persists Refund + PaymentAuditLog with ID-free metadata proof and raw idempotency-key storage denial, returns a safe receipt projection without tenant ID, Refund ID, Payment ID, BookingRequest ID, Deposit ID, provider refund ID, audit ID, idempotency-key ID, raw idempotency key, raw reason, or internal persistence ID echoes across DB success/replay, production/local fallback, payment-not-found, scope-invalid, and database-unavailable branches, and disables production local fallback. Stripe refund execution, webhook reconciliation, rollback, and settlement proof remain provider-gated. |

Dashboard refund-record receipts now suppress tenant, Refund, Payment, BookingRequest, Deposit, and provider refund identifiers plus raw reason text (`tenantIdEchoed=false`, `refundIdEchoed=false`, `paymentIdEchoed=false`, `bookingRequestIdEchoed=false`, `depositIdEchoed=false`, `providerRefundIdEchoed=false`, `rawReasonEchoed=false`) across DB success/replay, production/local fallback, payment-not-found, scope-invalid, and database-unavailable branches while preserving tenant-match proof, tenant-scoped persistence, and PaymentAuditLog metadata internally; Stripe refund execution, webhook reconciliation, rollback, and settlement proof remain provider-gated under `GAP-038`/`GAP-004`/`GAP-052`.
| `POST` | `api/intake/forms` | Create intake form | `settings:write` | `intakeFormInputSchema` | DB-backed route added: validates form shell, enforces tenant key/version uniqueness, claims/replays IdempotencyKey results, persists IntakeForm + AuditLog in one transaction, stores ID-free replay and AuditLog metadata proof (`intakeFormPersisted`/`auditLogged`/privacy blockers plus `internalPersistenceIdsStored=false`) with raw idempotency-key storage denial, reloads replay by tenant/key/version, returns safe success/duplicate receipt projections without audit ID, idempotency-key ID, raw idempotency key, existing form ID, or internal persistence ID echoes, and disables production local fallback. Question authoring, response persistence, privacy review, and integration tests remain evidence-gated. |
| `POST` | `api/consent/forms` | Create consent form | `settings:write` | `consentFormInputSchema` | DB-backed route added: validates consent form shell/body, enforces tenant key/version uniqueness, claims/replays IdempotencyKey results, persists ConsentForm + AuditLog in one transaction, stores ID-free replay and AuditLog metadata proof (`consentFormPersisted`/`auditLogged`/legal-signature-medical blockers plus `internalPersistenceIdsStored=false`) with raw idempotency-key storage denial, reloads replay by tenant/key/version, returns safe success/duplicate receipt projections without audit ID, idempotency-key ID, raw idempotency key, raw body, existing form ID, or internal persistence ID echoes, and disables production local fallback. Legal approval, signature/file workflows, medical acknowledgments, and integration tests remain evidence-gated. |

Intake and consent form creation success receipts now suppress form and tenant row IDs at the response boundary while preserving tenant-scoped form persistence and AuditLog metadata internally; question/response/privacy, legal/signature/file, medical-acknowledgment, and integration evidence remain gated under `GAP-038`/`GAP-040`.
| `PATCH` | `api/forms/:formId` | Archive form metadata | `form:write` | `archive_form_version` action contract | DB-backed metadata-only archive route: validates tenant/RBAC scope, updates IntakeForm or ConsentForm status to archived, persists IdempotencyKey + AuditLog result metadata with raw idempotency-key storage denial, stores ID-free replay proof (`formArchived`/`auditLogged` plus side-effect blockers and `internalPersistenceIdsStored=false`) instead of form/audit IDs, returns a no-store safe receipt without audit ID, idempotency-key ID, or internal persistence ID echoes, and disables production local fallback. Legal copy changes, signature requests, raw answers, medical payloads, private upload retention, completed-replay short-circuit tests, and integration tests remain evidence-gated. |
| `GET` | `/api/error-reports` | Error/crash reports | `error:read` | `errorReportFilterSchema` optional `tenantId`/`status`/`source`, bounded `limit` | DB-backed route exists: tenant-scoped, RBAC-gated, no-store, audit-logged, recursively metadata-redacted error report reads with no tenant, ErrorReport, audit, raw metadata, or internal persistence ID echoes on DB and fallback paths; production local fallback fails closed. Alert routing/provider escalation remains evidence-gated. |
| `POST` | `/api/error-reports` | Authenticated dashboard error-report ingest boundary | `error:write` | `errorReportInputSchema` | DB-backed route exists: authenticated tenant-scoped error ingest, no-store response with audit proof flags and no tenant, ErrorReport, AuditLog, raw metadata, or internal persistence ID echoes on DB and fallback paths, local fallback disabled in production, persisted ErrorReport + AuditLog on DB paths, and provider alert routing remains evidence-gated. |
| `POST` | `/api/observability/release-incidents` | Link release regressions to incident plans | `release:write` | Release incident linkage plan | DB-backed route exists: reads tenant-scoped ErrorReport/ReleaseRecord evidence, recursively redacts ErrorReport metadata before incident planning, persists ReleaseIncidentLink/AuditLog/ErrorReport metadata on allowed DB paths, stores ErrorReport release-linkage metadata as ID-free audit/link/release-record proof with `internalPersistenceIdsStored=false`, and returns no-store allowlisted responses with tenant, ErrorReport, AuditLog, ReleaseRecord, ReleaseIncidentLink, linked-report, and internal persistence ID no-echo proof flags on DB and fallback paths. Production provider-evidence failures now fail closed before AuditLog/ReleaseIncidentLink writes; live Sentry/source-map, provider incident, dashboard smoke, tenant isolation, and CI proof remain evidence-gated under `GAP-093`. |

Release incident artifact review now redacts release/report/linkage/audit selectors, fingerprints, routes, provider issue URLs, payloads, Sentry/webhook/provider credentials, and contact values before retained incident evidence is accepted; live Sentry release/source-map proof, migrated DB-backed linkage tests, dashboard smoke, tenant isolation, provider incident proof, and CI remain gated under `GAP-093`.
| `POST` | `/api/observability/github-issues` | Approve GitHub issue automation for an ErrorReport | `error:write` | Explicit human approval + persisted tenant-scoped `errorReportId` in production | Route exists: enforces dashboard RBAC, tenant matching, human approval, no-store responses, production synthetic-report fallback denial, recursive redacted ErrorReport metadata projection before issue planning, approval AuditLog persistence, GithubIssueLink/ErrorReport status seams, safe dispatch receipts with `issueLinked=false` instead of placeholder issue URL/number fields until GitHub returns a real issue URL, and GitHub provider dispatch behind runtime evidence plus `GITHUB_ISSUE_DISPATCH_ENABLED`. Live GitHub dispatch, migrated DB smoke, CI evidence, and secret-safe artifacts remain evidence-gated under `GAP-085`. |

GitHub issue automation artifact review now redacts tenant/report/link/audit/idempotency selectors, repository and installation identifiers, issue URLs/numbers, workflow URLs, provider payloads, request bodies, credentials, contacts, and stack traces before retained dispatch evidence is accepted. This is local/static artifact-safety proof only; live synthetic GitHub issue creation, migrated DB smoke, provider-backed secret-safe artifacts, and CI evidence remain gated under `GAP-085`.
| `POST` | `/api/releases` | Create release note / rollback intent | `release:write` | `releaseCreateInputSchema`; GET uses `releaseTenantQuerySchema`; rollback uses `releaseRollbackInputSchema` | DB-backed route exists: tenant/membership-gated release reads and release creation/rollback intents with idempotency-backed persistence and audit metadata, ID-free IdempotencyKey proof (`releasePersisted`/`auditLogged`/rollback flags plus `internalPersistenceIdsStored=false`), ID-free release AuditLog metadata proof with raw idempotency-key storage denial and no release-record ID in orchestration metadata, safe read/create/rollback receipt projections without tenant ID, audit ID, idempotency-key ID, source release-record ID, release-record ID, commit SHA, or internal persistence ID echoes; deployment automation remains external/evidence-gated. |
| `PATCH` | `/api/releases` | Request rollback intent | `release:write` | `releaseRollbackInputSchema` | DB-backed route exists: tenant/membership-gated rollback intent creation with idempotency-backed AuditLog metadata, ID-free rollback result proof with `internalPersistenceIdsStored=false`, no-store tenant-scope/no-tenant-ID response projections, and response-level proof that provider rollback/deployment execution did not run; protected-environment rollback remains external/evidence-gated. |
| `PATCH` | `/api/feature-flags` | Toggle tenant feature flag | `settings:write` | `featureFlagPatchInputSchema`; GET uses `featureFlagReadQuerySchema` | Implemented as route-backed feature flag writes via `POST /api/feature-flags`: tenant/membership-gated, idempotency-backed DB override persistence with request-hash conflict denial, completed replay proof that stores `featureFlagPersisted`/`auditLogged` plus `internalPersistenceIdsStored=false` instead of FeatureFlag/AuditLog IDs, ID-free AuditLog metadata proof with raw idempotency-key storage denial, replay reload by tenant/key, no-store safe read/write receipt projections without tenant ID, audit ID, idempotency-key ID, raw idempotency key, concurrency record ID, raw provider payload, tenant-derived cache keys, or internal persistence ID echoes, and provider-credential gates for live provider flags. Contract path should be normalized in consumers; CI/RBAC evidence remains gated. |
| `GET` | `/api/settings` | Tenant settings read | `tenant:read` | `dashboardTenantQuerySchema` optional `tenantId` | DB-backed route exists: tenant-scoped Tenant settings read with domains, members, custom roles, studios, and feature flags; member/user invite email/name contact fields are not selected from the database and response fields use redacted presence indicators, domain verification token hashes and studio street addresses are not selected, no-store safe read receipt projection suppresses tenant, domain, member, user, custom-role, studio, feature-flag, audit, and internal persistence ID echoes, and AuditLog is written. Production local fallback fails closed; provider secrets/member invite workflows remain evidence-gated. |
| `PATCH` | `/api/settings` | Update safe tenant settings metadata | `settings:write` | `tenantSettingsMutationSchema` plus `dashboardTenantQuerySchema` for GET | DB-backed settings write exists: validates tenant/RBAC scope, allows only public site name/locale/timezone metadata, persists Tenant update + IdempotencyKey request-hash conflict denial + ID-free completed replay proof + AuditLog metadata with raw idempotency-key storage denial, replays with `auditLogged` proof instead of AuditLog ID reconstruction, returns no-store safe mutation receipt projection without tenant ID, Tenant row ID, audit ID, idempotency-key ID, raw idempotency key, raw secret, or internal persistence ID echoes across DB success/replay, idempotency-conflict, production/local fallback, and database-unavailable branches, and disables production local fallback. Provider secrets, credentials, legal policy copy, member invites, custom roles, and integration proof remain gated. |

Settings read and mutation receipts now suppress raw tenant and Tenant row identifiers at the response boundary (`tenantIdEchoed=false`, `tenantRecordIdEchoed=false`) across DB success/replay, idempotency-conflict, production/local fallback, and database-unavailable branches while preserving tenant-match proof, tenant-scoped reads, metadata persistence, and AuditLog proof internally; provider secrets, member/custom-role workflows, legal policy copy, and integration evidence remain gated under `GAP-038`/`GAP-040`.
| `GET` | `/api/messages` | Tenant message threads and delivery logs | `message:read` | `dashboardListQuerySchema` optional `tenantId`, bounded `limit` | DB-backed route exists: tenant-scoped message thread list/detail reads with message body/provider IDs and client email/phone not selected from the database, fixed redaction sentinels, no-store safe read projection without audit ID or internal persistence ID echoes, and AuditLog write; production local fallback fails closed. Provider delivery evidence remains gated. |
| `POST` | `/api/messages` | Create dashboard message thread/message rows | `message:write` | Dashboard message persistence plan | DB-backed route exists: validates tenant/RBAC scope, persists MessageThread/Message/Notification/Delivery/ReadState/Handoff/AuditLog rows transactionally, stores SHA-256 fingerprinted provider-handoff idempotency correlation plus ID-free sanitized payload proof and a safe ID-free idempotency result with `internalPersistenceIdsStored=false`, and returns only a redacted message-write plan projection with raw message body, destination hash, idempotency key, and internal persistence ID echoes disabled. Provider sends and seeded integration proof remain evidence-gated. |
| `GET` | `/api/messages` | Dashboard message thread list | `message:read` | `dashboardListQuerySchema` optional `tenantId`, bounded `limit` | DB-backed route exists: tenant-scoped MessageThread list read with message body/provider/contact redaction, no-store responses, AuditLog write, and safe read projection without thread, tenant, client, booking, appointment, latest-message, sender, audit, or internal persistence ID echoes. Production local fallback fails closed; provider sends/replay, suppression reconciliation, seeded route evidence, and CI remain gated. |
| `GET` | `/api/reviews` | Dashboard review moderation queue | `review:read` | Optional `tenantId`, `status`, bounded `limit` | DB-backed route exists: tenant-scoped review queue reads with raw review bodies and private client/booking references not selected from the database, fixed body redaction sentinels, no-store safe read projection without tenant, Review, artist, audit, or internal persistence ID echoes on DB and fallback paths, and AuditLog write; production local fallback fails closed. Publication workflow/provider proof remains gated. |
| `POST` | `/api/messages/:threadId` | Reply to client thread | `client:write` | `messageInputSchema` plus message route metadata | Implemented through dashboard message write route: claims IdempotencyKey, creates tenant-scoped MessageThread/Message/Notification/Delivery/ReadState/Handoff/AuditLog transactionally, returns no-store safe write projections without tenant ID, thread/message/notification/delivery/read-state/status-transition/provider-handoff/audit IDs, raw message body, raw destination hash, raw idempotency key, or internal persistence ID echoes across blocked, production/local fallback, DB success, duplicate, and database-unavailable branches, and keeps provider dispatch evidence-gated. |
| `POST` | `api/notifications/preview` | Preview template delivery plan | `settings:write` | `notificationPreviewInputSchema` | Implemented as no-persistence delivery-plan preview: validates template context/consent, returns a safe plan projection with template/candidate status metadata only, does not echo rendered bodies, raw template context, destination masks, provider payloads, tenant IDs, or internal persistence IDs (`tenantIdEchoed=false`, `internalPersistenceIdsEchoed=false`), uses no-store responses, and keeps provider dispatch/sandbox evidence gated. |
| `POST` | `api/notifications/scheduler` | Schedule/process notification jobs | `message:write` | Scheduler action payload | DB-backed `schedule_sequence` path exists for dashboard actors with no-store responses, IdempotencyKey request-hash conflict denial, Notification/NotificationDelivery/NotificationProviderHandoff/AuditLog persistence, SHA-256 fingerprinted provider-handoff/audit idempotency correlation, ID-free stored proof with `internalPersistenceIdsStored=false`, worker audit idempotency proof stores `notificationJobPersisted` instead of job IDs, and a safe scheduler-plan projection that does not echo raw scheduled jobs, write payloads, job IDs, actor IDs, cancellation reasons, idempotency keys, audit IDs, or internal persistence IDs. Worker execution, retry/dead-letter/cancellation execution, provider dispatch, seeded concurrency, CI, and secret-safe artifacts remain evidence-gated under `GAP-065`/`GAP-066`. |
| `POST` | `api/notifications/queue` | Queue consent-aware notification | `settings:write` or domain permission | `notificationInputSchema` plus delivery plan | DB-backed route added: validates notification input, enriches consent from tenant-scoped client records, claims/replays IdempotencyKey results with request-hash conflict denial, persists Notification + NotificationDelivery + NotificationProviderHandoff + AuditLog rows with hash-only destination metadata, SHA-256 fingerprinted provider-handoff idempotency correlation, and ID-free handoff/idempotency metadata proof, stores ID-free replay proof (`notificationPersisted`/delivery/handoff/audit/provider-deferred flags plus counts and `internalPersistenceIdsStored=false`) instead of Notification/Delivery/Handoff/AuditLog IDs, no persisted/echoed body previews, safe receipt projection without notification ID, delivery IDs, handoff IDs, audit ID, idempotency-key ID, raw idempotency key/result, raw destinations, body preview, or internal persistence ID echoes, and disables production local fallback. Provider workers/sends, retries, dead letters, and sandbox/device evidence remain gated. |
Dashboard notification queue receipts now also suppress tenant identifiers (`tenantIdEchoed=false`) on DB success/replay, idempotency-conflict, database-unavailable, and local-fallback fail-closed paths while preserving hash-only destination metadata, counts, provider-dispatch deferred proof, and no-store headers; provider workers/sends, retries, dead letters, sandbox/device sends, route tests, and CI remain gated under `GAP-010`/`GAP-038`/`GAP-065`/`GAP-069`.

Dashboard notification preference update receipts now suppress tenant and Client row IDs (`tenantIdEchoed=false`, `clientIdEchoed=false`) along with contact fields, raw destinations, destination hashes, preference/suppression row IDs, audit/idempotency IDs, and internal persistence IDs across database success/replay, client-not-found, production/local fallback, and database-unavailable branches while preserving tenant-match proof; provider webhook replay, live STOP enforcement, route execution, and CI evidence remain gated under `GAP-038`/`GAP-010`/`GAP-067`.
| `POST` | `api/notifications/scheduler` | Persist notification schedule-sequence jobs | `message:write` | Scheduler action plan | DB-backed `schedule_sequence` route added: validates scheduler plan, verifies related booking/appointment tenant scope, claims/replays IdempotencyKey results with request-hash conflict denial, persists Notification + NotificationDelivery + NotificationProviderHandoff + AuditLog rows with provider dispatch disabled, stores fingerprinted handoff/audit idempotency correlation plus no-echo audit/idempotency/internal-ID proof flags with `internalPersistenceIdsStored=false` and raw request-hash duplication denial in metadata/result, and disables production local fallback. Process/retry/dead-letter worker execution, queue concurrency, provider dispatch, and integration evidence remain gated under `GAP-065`/`GAP-066`. |
| `PATCH` | `api/notification-preferences/:clientId` | Update preferences/suppression state | `client:write` | `notificationConsentInputSchema` | DB-backed route added: validates client path/body match, claims/replays IdempotencyKey results, updates Client opt-in flags, upserts NotificationChannelPreference and NotificationSuppression rows with hashed destinations, writes hash-only/no-raw-destination and ID-free suppression/audit metadata, returns a safe receipt projection without tenant ID, client ID, raw contact, raw destination, destination hash, preference/suppression row IDs, audit ID, idempotency-key ID, raw idempotency key, or internal persistence ID echoes, stores ID-free idempotency replay proof (`preferencesPersisted`/`suppressionsEvaluated`/`auditLogged`/counts plus `internalPersistenceIdsStored=false`), and disables production local fallback. Provider webhook replay, live STOP enforcement, and integration evidence remain gated. |

Messaging privacy artifact review now redacts message bodies, attachment URLs, provider payloads, export/delete/retention/moderation evidence, rate-limit/audit/idempotency fields, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/customer labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained messaging privacy evidence is accepted. This remains a local/static artifact-safety contract only; role-visibility/API authorization tests, secure attachment integration, retention/export/delete Postgres tests, moderation/rate-limit execution, CI proof, and redacted artifact capture remain gated under `GAP-068`.

Notification persistence artifact review now redacts message bodies, notification/delivery/destination payloads, provider handoff and read-state/status-transition evidence, audit/idempotency fields, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/customer labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained notification persistence evidence is accepted. This remains a local/static artifact-safety contract only; Prisma migration/client generation, seeded durable notification persistence, cross-tenant isolation, provider dispatch, CI proof, and redacted artifact capture remain gated under `GAP-064`.

Notification scheduler artifact review now redacts scheduled job payloads, message destinations, provider dispatch evidence, worker audit/dead-letter/retry payloads, queue backend details, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/customer labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained scheduler evidence is accepted. This remains a local/static artifact-safety contract only; worker execution, retry/dead-letter/cancel execution, provider dispatch, Postgres concurrency, route tests, CI proof, and redacted artifact capture remain gated under `GAP-065`.

Travel publish artifact review now redacts travel publish/waitlist/notification queue payloads, sync/rollback/E2E traces, screenshots, provider/cache/revalidation evidence, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/customer labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained travel publish evidence is accepted. This remains a local/static artifact-safety contract only; durable repository execution, public API reads, cache revalidation, waitlist matching, provider fanout, dashboard-to-public E2E, rollback tests, CI proof, and redacted artifact capture remain gated under `GAP-060`.

## Mobile API use

The mobile app should consume tenant-scoped dashboard APIs. A thin mobile BFF can be added only if app-specific payload shapes become meaningfully different. Mobile endpoints must use the same auth/RBAC rules and should never receive private data beyond the signed-in artist's tenant scope.

Mobile API runtime artifact review now redacts provider token exchange evidence, tenant-scoped request/replay payloads, offline idempotency labels, device smoke evidence, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/user/member labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained mobile API evidence is accepted. This remains a local/static artifact-safety contract only; provider login/token exchange, seeded API smoke, expired-auth denial, cross-tenant denial, offline reconnect/replay execution, mobile typecheck/tests, device smoke, CI proof, and redacted artifact capture remain gated under `GAP-043`.

## Webhook endpoints planned

| Method | Route | Purpose | Auth | Status |
| --- | --- | --- | --- | --- |
| `POST` | `/api/webhooks/stripe` | Stripe payment/deposit status | Stripe signature | DB-first reconciliation boundary exists: requires Stripe signature header, requires `STRIPE_WEBHOOK_SECRET` before production processing, verifies HMAC when configured, resolves tenant metadata, persists redacted `ProviderWebhookDelivery` replay rows with ID-free sanitized payload proof (`rawProviderEventIdStored=false`, `internalPersistenceIdsStored=false`) plus ID-free Payment/PaymentAuditLog metadata proof, updates matching Payment/Deposit status only when tenant-scoped metadata/provider IDs and money checks allow it, and returns allowlisted persistence/local receipts with tenant slug/scope proof plus payment/deposit mutation proof and status only, no raw tenant ID, raw local webhook row, raw mutation rows, raw payload, provider object, reconciliation idempotency-key, Payment/Deposit ID, ProviderWebhookDelivery ID, AuditLog ID, raw Stripe event ID, raw interpretation object, raw webhook contract, or internal persistence ID response echo. Production fail-closed responses also suppress raw provider event IDs and provider payloads. Local runtime fallback remains non-production only; Stripe CLI/provider proof remains evidence-gated. |

Stripe webhook production fail-closed responses now return tenant-scope proof only (`tenantResolved`, `tenantSlugEchoed=false`) instead of echoing the resolved tenant slug, while preserving safe interpretation and provider/reconciliation no-echo flags. Live Stripe CLI/provider replay, webhook secret evidence, tenant reconciliation, and CI proof remain gated under `GAP-050`.
| `POST` | `/api/webhooks/sentry` | Optional issue sync and provider issue reconciliation | Sentry signature/secret header | DB-backed reconciliation boundary exists: requires Sentry signature header, verifies HMAC when `SENTRY_WEBHOOK_SECRET` is configured, records provider delivery with a hashed provider delivery ID and SHA-256 idempotency fingerprint, reconciles matching `ErrorReport` status, writes `AuditLog` with ID-free delivery/report/idempotency proof plus `rawProviderDeliveryIdStored=false`, `rawIdempotencyKeyStored=false`, and `internalPersistenceIdsStored=false`, returns a hashed `providerDeliveryFingerprint` only with no raw provider delivery ID, idempotency-key/provider-payload, ProviderWebhookDelivery ID, AuditLog ID, matched ErrorReport ID, or internal persistence ID response echoes, and fails closed in production when durable tenant ownership/persistence is unavailable. Live Sentry replay/no-PII/provider proof remains evidence-gated. |

Sentry provider webhook retained-artifact review now uses `buildRedactedProviderWebhookReconciliationArtifact` before persistence/review packets, redacting provider payloads, neutral artifact labels, CI/workflow/run/commit selectors, URLs, paths, tokens, credentials, and contact values. This is a local/static artifact-safety contract only; live Sentry replay, no-PII artifact audit execution, CI evidence, and provider-backed persistence proof remain gated under `GAP-082`.

Encryption/key-management evidence packets now have `buildRedactedEncryptionKeyEvidenceArtifact` for GAP-021 handoff artifacts. The helper redacts key IDs, cache/rotation labels, provider-token readiness selectors, env-var names, URLs, artifact paths, credentials, contacts, ciphertext/plaintext-shaped fields, and CI selectors before evidence retention; runtime key material, provider-token operational rotation, and live credential governance remain external evidence gates under `GAP-021`/`GAP-034`.

Public booking runtime evidence for GAP-017 is retained through `buildRedactedBookingFlowRuntimeArtifact`, which redacts workflow traces, anti-bot/body-hash selectors, route/request/response payloads, raw medical notes, payment/provider tokens, private file URLs, database selectors, CI/run/commit labels, and artifact paths before booking-flow evidence is handed off. The DB/local route behavior remains as described above; future anti-bot recovery and provider failure-mode hardening are tracked under `GAP-034`.

Calendar launch evidence for GAP-009 is retained through `buildRedactedCalendarLaunchArtifact`/`buildCalendarLaunchArtifactReview`, which redact Google OAuth/access/refresh tokens, signed ICS feed URLs/tokens, calendar provider selectors, tenant/account IDs, database URLs, client contact values, CI selectors, and calendar artifact paths before tracker or handoff use. Live Google OAuth/sync, signed-feed import execution, Postgres race/isolation proof, CI evidence, and provider-backed `CalendarLaunchRun` persistence remain gated.

Dashboard test evidence for GAP-041 is retained through `buildRedactedDashboardTestArtifact`/`buildDashboardTestArtifactReview`, which redact dashboard auth/session tokens, tenant/client labels, route payloads, browser traces, screenshots/videos, branch-protection selectors, CI/run/commit labels, URLs, and artifact paths before evidence handoff. Runnable dashboard tests, accessibility/keyboard checks, Playwright critical flows, branch protection proof, flaky policy, and CI artifacts remain gated.

Availability persistence evidence for GAP-056 is retained through `buildRedactedAvailabilityPersistenceArtifact`/`buildAvailabilityPersistenceArtifactReview`, which redact tenant/client/artist identifiers, calendar/availability/hold tokens, database URLs, audit/idempotency/conflict selectors, CI/run labels, and artifact paths before secret-safe review. Seeded Postgres lifecycle proof, overlap/race execution, cross-tenant mutation denial, dashboard API repository proof, CI artifacts, and production write evidence remain gated.

Booking lifecycle/state-machine evidence for GAP-035 is retained through `buildRedactedBookingLifecycleEvidenceArtifact`, which redacts tenant/booking/actor IDs, idempotency keys, audit/event selectors, provider/payment tokens, raw medical/private-file values, CI selectors, URLs, and artifact paths before lifecycle evidence handoff. Current lifecycle transitions remain locally covered; future test expansion is only required when new lifecycle states/actions are introduced.
| `POST` | `/api/webhooks/calendar` | Google calendar updates | Provider verification | Local boundary route added: validates Google push headers, builds an incremental-sync plan with provider execution blocked, returns no-store local receipt outside production without raw provider channel/resource/message/token, tenant, idempotency-key, or internal persistence ID echoes, and fails closed in production until OAuth credentials, encrypted token persistence, verified channel tokens, sync worker execution, idempotency persistence, and CalendarAuditLog proof exist. |
| `POST` | `/api/webhooks/email` | Email delivery status | Provider signature | Local boundary requires signature-like header, validates JSON, computes readiness/reconciliation plans, attempts redacted `ProviderEvent` + `IdempotencyKey` + `AuditLog` persistence on DB-available non-production paths, stores fingerprinted provider event/message correlation plus ID-free IdempotencyKey result and provider/audit metadata proof with `rawIdempotencyKeyStored=false` and `internalPersistenceIdsStored=false`, upserts `NotificationSuppression` for bounce/complaint/unsubscribe payloads that include a destination, returns no raw provider-signature, provider event/message ID, tenant slug/ID, idempotency-key, ProviderEvent, AuditLog, delivery, transition, suppression, or internal persistence ID response echoes, and persists interpreted local runtime state. Production fails closed before durable writes until cryptographic provider signature verification, live replay, suppression reconciliation, and integration evidence exist. |
| `POST` | `/api/webhooks/sms` | SMS delivery/inbound messages and STOP preview | Provider signature | Local boundary requires Twilio signature-like header, supports JSON/form payloads, computes readiness/reconciliation plans, attempts redacted `ProviderEvent` + `IdempotencyKey` + `AuditLog` persistence on DB-available non-production paths, stores fingerprinted provider event/message correlation plus ID-free IdempotencyKey result and provider/audit metadata proof with `rawIdempotencyKeyStored=false` and `internalPersistenceIdsStored=false`, upserts `NotificationSuppression` for STOP payloads with a sender, returns no raw inbound body, provider event/message ID, tenant slug/ID, provider-signature, idempotency-key, ProviderEvent, AuditLog, delivery, transition, suppression, or internal persistence ID response echoes, and returns an explicit inbound-thread boundary when no tenant client can be resolved. Production fails closed before Twilio signature verification, live replay, STOP/inbound reconciliation, and integration evidence exist. |

## State transition rules

Booking state changes must create a `BookingStateEvent`. Payment and deposit changes must create `PaymentAuditLog`. Sensitive changes should also create an `AuditLog` row.

Examples:

- `submitted -> accepted` requires `booking:write`.
- `accepted -> deposit_pending` requires a deposit record.
- `deposit_pending -> deposit_paid` should be driven by verified Stripe webhook in production (gated/proof pending).
- `deposit_paid -> scheduled` requires an appointment record.
- `scheduled -> completed` should trigger aftercare notification scheduling.
- `scheduled -> no_show` should preserve no-show audit metadata.

## Phase 2 implementation note

The schema and validators are expanded, and booking request handling is now production-oriented with DB persistence, anti-bot controls, encryption gates, and workflow handoff contracts. Other public handlers are mixed: several are DB-first with production local-fallback guards, while provider-backed execution and tenant-isolated integration evidence remain tracked under their owning GAP rows.


## Phase 3 implementation note

The public website now has static demo city/style routes plus DB-oriented public content API handlers for portfolio, travel, approved reviews, published city SEO pages, and published style SEO pages. A public FAQ bundle route is also wired for tenant-safe public content, while durable FAQ CMS persistence remains tracked under SEO/public-content gaps. These handlers resolve tenant scope, expose only public/redacted fields, and use non-production demo fallback only when DB content is unavailable. Capture API JSON/rendered HTML redaction, cache, route smoke, browser, and CI evidence before production closure.

Public content read receipts now suppress tenant, internal persistence, private-field, and raw local runtime record echoes (`tenantIdEchoed=false`, `internalPersistenceIdsEchoed=false`, `rawPrivateFieldsEchoed=false`, `rawLocalRuntimeRecordEchoed=false`) on DB portfolio/reviews/travel/city/style paths and local fallback bundle paths while preserving tenant slug, public collection data, redacted-field lists, and cache policy; persisted CMS seed proof, rendered HTML/API redaction artifacts, cache revalidation proof, browser smoke, and CI remain gated under `GAP-026`/`GAP-027`/`GAP-028`/`GAP-076`.


## Phase 4 implementation note
`apps/web/app/api/public/[tenantSlug]/booking-requests/route.ts` now includes production-oriented persistence behavior in the DB path:

- Parses JSON.
- Validates against `bookingRequestInputSchema`.
- Resolves tenant by slug, then applies fallback behavior when DB is unavailable.
- Applies local rate limit and enforces anti-bot proof (`x-inkroute-bot-proof`) for DB-backed writes; DB persistence is denied with `BOT_PROTECTION_REQUIRED` when proof is absent/invalid, and response metadata exposes only status/proof flags (`bodyHashEchoed=false`) rather than request body hashes.
- Persists `BookingRequest`, `BookingStateEvent`, and `AuditLog` rows to Postgres when DB is available.
- Enforces key-policy checks for sensitive persistence and emits encryption readiness, rotation-state/action, round-trip proof, key-cache refresh evidence, and provider-token readiness in response metadata + audit payloads.
- Produces scope-aware post-persist workflow contracts (`notification`, `deposit`, `calendar`, `reference-upload`) with signed reference-upload handoff contract fields for downstream queue/worker handoff, returns tenant-scope proof instead of raw tenant IDs, sanitizes workflow records/local consumer results/provider-failure audit payloads with `tenantIdEchoed=false`/`bookingRequestIdEchoed=false`/`workflowRecordIdEchoed=false`, and persists redacted provider-handoff audit metadata on DB-backed booking writes.
- Captures provider-token-intake detection metadata and blocks DB persistence when encryption readiness is invalid for token-bearing payloads.
- Returns public booking receipts through an allowlist that suppresses BookingRequest, tenant, artist, client, travel-city, portfolio-attribution, and internal persistence identifiers (`bookingRequestIdEchoed=false`, `tenantIdEchoed=false`, `artistIdEchoed=false`, `clientIdEchoed=false`, `travelCityIdEchoed=false`, `portfolioAttributionIdEchoed=false`, `internalPersistenceIdsEchoed=false`) while preserving public status/preference/readiness fields.
- Falls back to local runtime draft persistence when DB is unavailable, with matching DB/local workflow plan shapes and ID-free local consumer results that expose `messagePersisted`, `uploadIntentPersisted`, and `signedUploadUrlEchoed=false` instead of local message IDs, upload intent IDs, signed upload URLs, raw payloads, or internal persistence IDs.

Do not expose this as a complete production booking endpoint until `GAP-031`, `GAP-032`, `GAP-033`, `GAP-034`, `GAP-061`, and tenant-isolated integration tests are completed; `GAP-021` key-policy/encryption controls are locally closed, while provider-token operational hardening remains tracked under the provider handoff gaps.

Booking flow runtime artifact review now redacts booking route request/response payloads, local runtime consumer output, workflow/state-event/audit selectors, anti-bot proof tokens, reference/upload metadata, browser smoke traces/screenshots/videos, rendered confirmation artifacts, DB transaction smoke output, command output, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/booking/payment/medical labels, contacts, credentials, tokens, and authorization headers before retained booking-flow evidence is accepted. This remains a local/static artifact-safety contract only; dependency install, Prisma Client generation, web typecheck/build, booking route contract tests, Next public API runtime smoke, `/booking` and `/booking/confirmation` browser smokes, dev-DB transaction smoke, CI proof, secret-safe artifact review, and provider-backed `BookingFlowRuntimeRun` persistence remain gated under `GAP-031`.

Booking persistence API artifact review now redacts public booking route request/response payloads, anti-bot proof tokens, encryption-policy/sensitive-write evidence, DB transaction smoke output, local fallback and workflow consumer payloads, BookingRequest/BookingStateEvent/AuditLog selectors, Prisma connection details, Next route runtime transcripts, command output, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/booking/payment/medical labels, contacts, credentials, tokens, and authorization headers before retained booking-persistence evidence is accepted. This remains a local/static artifact-safety contract only; generated Prisma Client, provider-backed `BookingPersistenceApiRun` persistence, dev-DB transaction smoke, web typecheck/build, Next route runtime smoke, fresh CI proof, and secret-safe artifact review remain gated under `GAP-032`.

Booking provider handoff artifact review now redacts provider-worker payloads, Stripe deposit sandbox transcripts, notification/SMS/push delivery logs, Google Calendar tentative holds, reference-upload worker output, webhook signatures, audit/idempotency/replay proof, retry/rollback/operator-review transcripts, provider sandbox request/response bodies, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/booking/deposit/payment/calendar/notification labels, contacts, credentials, tokens, and authorization headers before retained provider-handoff evidence is accepted. This remains a local/static artifact-safety contract only; live Stripe sessions, notification queue delivery, calendar holds, durable provider workers, live provider audit payload execution, idempotency/replay proof, rollback/retry/operator-review integration, sandbox transcripts, fresh CI, and redacted artifact capture remain gated under `GAP-034`.


## Phase 5 implementation note

`apps/dashboard` now contains tenant-scoped API handlers for the core dashboard read/mutation surface listed above, with shared validators, RBAC guards, no-store responses, redaction, idempotency, and audit metadata where implemented. Remaining production work is evidence-gated around provider-backed auth/session proof, seeded tenant-isolation/RBAC tests, provider execution, dashboard build/typecheck, CI artifacts, and secret-safe evidence capture. Future dashboard endpoints must continue replacing static demo data from `apps/dashboard/lib/demo.ts` with authenticated, tenant-scoped API/server loaders and mutation handlers.

Auth/session tenant-guard artifact review now recursively redacts auth selectors before evidence retention, including tenant, actor/user, TenantMember/membership, custom role, provider user/account, revocation, audit, idempotency identifiers, provider callback URLs, database DSNs, and artifact paths in addition to tokens, sessions, credentials, contact values, raw bodies, and stacks. This is local/static artifact-safety proof only; provider-backed login/logout, persisted session/revocation, cross-tenant route integration, and CI evidence remain gated under `GAP-095`.

The shared dashboard actor resolver disables header-only dashboard actors in production with `AUTH_REQUIRED`; local/header preview behavior remains non-production only until provider-backed session and persisted TenantMember/CustomRole evidence is captured.

Do not treat the Phase 5 dashboard UI as production-ready until `GAP-036`, `GAP-037`, `GAP-038`, `GAP-039`, `GAP-040`, and `GAP-041` are resolved.

Dashboard build/runtime artifact review now redacts install/build/typecheck/test output, Next/React/JSX runtime traces, rendered dashboard HTML/DOM, browser smoke traces/screenshots/videos, route/API payloads, source maps, environment/header values, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/booking/payment/portfolio/message labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained dashboard build evidence is accepted. This remains a local/static artifact-safety contract only; dependency install, React/JSX/Next type verification, dashboard typecheck/build/test, browser route smoke, Next 15 runtime smoke, fresh CI proof, and redacted artifact capture remain gated under `GAP-039`.

Dashboard test runtime artifact review now redacts route/auth/RBAC/mutation/accessibility/browser/CI evidence payloads, trace/video/screenshot references, branch-protection proof, command output, CI/workflow/run selectors, artifact paths, provider/session fields, tenant/client/booking/payment/portfolio/message labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained dashboard test evidence is accepted. This remains a local/static artifact-safety contract only; runnable dashboard unit/component tests, route/auth/RBAC/tenant-isolation fixtures, booking lifecycle mutation tests, provider-safe states, axe/keyboard checks, Playwright critical flows, CI artifact upload proof, branch-protection evidence, flaky-test policy, persisted run payload capture, and redacted artifact capture remain gated under `GAP-041`.

Mobile auth runtime artifact review now redacts provider login/logout/refresh evidence, SecureStore snapshots, biometric/device identifiers, tenant/RBAC denial payloads, audit/session fields, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/user/member labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained mobile auth evidence is accepted. This remains a local/static artifact-safety contract only; provider-backed session persistence, SecureStore/device plaintext-denial proof, biometric device smoke, tenant denial proof, audit persistence, CI proof, persisted run payload capture, and redacted artifact capture remain gated under `GAP-042`.

Mobile crash runtime artifact review now redacts Sentry DSNs/tokens, source-map/debug-symbol upload evidence, forced crash payloads, device identifiers, ErrorReport/dashboard triage payloads, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/user labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained mobile crash evidence is accepted. This remains a local/static artifact-safety contract only; live Sentry Expo SDK credentials, source-map/debug-symbol upload, forced simulator/device crash proof, sanitized ErrorReport persistence execution, dashboard triage sync, offline buffering proof, no-PII provider payload proof, CI proof, and redacted artifact capture remain gated under `GAP-046`.

Mobile offline sync runtime artifact review now redacts SecureStore/SQLite queue evidence, encrypted payloads, idempotency/conflict/audit labels, reconnect/device proof, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/user labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained offline sync evidence is accepted. This remains a local/static artifact-safety contract only; encrypted storage device proof, reconnect/replay execution, conflict resolution, audit persistence, mobile typecheck/tests, device smoke, CI proof, and redacted artifact capture remain gated under `GAP-045`.

Mobile OTA updates runtime artifact review now redacts EAS/Expo project credentials, update/channel/runtime identifiers, rollback/release-health/adoption evidence, device receipt labels, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/user labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained OTA evidence is accepted. This remains a local/static artifact-safety contract only; EAS project configuration, preview update publish, rollback republish, release-health/adoption monitoring, device proof, CI proof, and redacted artifact capture remain gated under `GAP-047`.

Mobile QA runtime artifact review now redacts device/simulator/emulator identifiers, screenshots/videos, auth/API/offline/push/crash/OTA/accessibility evidence, provider transcripts, CI/workflow/run selectors, artifact paths, tenant/client/user labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained QA evidence is accepted. This remains a local/static artifact-safety contract only; simulator/emulator/device QA execution, accessibility proof, provider-backed push/crash/API/OTA evidence, CI proof, and redacted artifact capture remain gated under `GAP-048`.

Mobile push runtime artifact review now redacts Expo/APNS/FCM credentials, device and push tokens, notification delivery/receipt/interaction evidence, deep-link routing labels, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/user labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained push evidence is accepted. This remains a local/static artifact-safety contract only; Expo credentials, APNS/FCM provider proof, durable token/audit persistence, delivery receipts, tap routing/device QA, CI proof, and redacted artifact capture remain gated under `GAP-063`.

Dashboard auth guard artifact review now redacts provider session evidence, cookies, CSRF/header values, TenantMember and CustomRole lookup payloads, route-method permission traces, middleware/layout/API-helper failure bodies, unauthorized/login/logout/tenant-switch/cross-tenant denial transcripts, browser auth traces/screenshots/videos, auth AuditLog rows, no-store/cache proof payloads, command output, CI/workflow/run selectors, artifact paths, provider/database fields and DSNs, tenant/user/client/account labels, contacts, credentials, tokens, and authorization headers before retained dashboard auth evidence is accepted. This remains a local/static artifact-safety contract only; provider-backed session callbacks, persisted TenantMember/CustomRole lookup proof, browser login/logout and tenant-switch proof, cross-tenant denial proof, auth AuditLog persistence, dashboard typecheck/build, fresh CI proof, and redacted artifact capture remain gated under `GAP-036`.

## Phase 6 mobile implementation note

`apps/mobile` now consumes static demo data and shared helper packages only. No mobile API client, authenticated session, refresh token handling, push token registration, offline sync endpoint, or mobile-specific BFF exists. Mobile should initially reuse dashboard tenant-scoped APIs for bookings, appointments, clients, travel, portfolio, notifications, errors, and releases. Add a mobile BFF only if payload shape, offline sync, or bandwidth constraints justify it.

Mobile production endpoints must enforce the same handler pipeline documented above: auth, tenant membership, RBAC, Zod validation, tenant-scoped Prisma queries, audit logging, and PII/medical redaction.

## Phase 7 payment route boundaries

### `POST /api/public/[tenantSlug]/deposit-sessions`

Status: **DB-first deposit/payment draft implemented; production SDK flow remains credential-gated**.

Purpose:
- Calculate a deposit policy preview for an accepted booking or signed deposit handoff.
- Return a redacted Stripe Checkout session draft/receipt with reconciliation metadata but no raw idempotency key, customer email, mock checkout URL, or provider session ID.
Avoid live payment collection until auth/signed-token, Stripe credentials, provider checkout creation, and webhook proof are wired (gated/proof pending).

Current implementation:
- Parses JSON manually in `apps/web/app/api/public/[tenantSlug]/deposit-sessions/route.ts`.
- Requires `bookingRequestId`, `successUrl`, and `cancelUrl`.
- Resolves the tenant/booking from the database first, rate-limits by tenant/client IP, verifies client email when supplied, and persists `Deposit`, pending `Payment`, `PaymentAuditLog`, `BookingStateEvent`, and `IdempotencyKey` in one transaction before returning a provider-disabled checkout draft with `rawIdempotencyKeyEchoed=false` and no deposit/payment/audit/internal persistence ID echoes; completed replay stores proof flags (`depositPersisted`/`paymentPersisted`/`paymentAuditPersisted`/`bookingStateEventPersisted` plus `internalPersistenceIdsStored=false`) and reloads by tenant/booking/amount/currency/status instead of storing internal row IDs.
- Payment, payment-audit, booking-state, and idempotency metadata store only ID-free booking/idempotency/deposit/payment proof with `internalPersistenceIdsStored=false`; raw idempotency keys, checkout URLs, BookingRequest IDs, Deposit IDs, and Payment IDs are not duplicated into metadata.
- Falls back to local runtime preview only outside production when database persistence is unavailable; local preview responses redact the mock provider session ID/checkout URL and customer email, with `GAP-004`, `GAP-049`, and `GAP-050` listed as provider/evidence blockers.

Production requirements:
1. Require authenticated dashboard action or a signed short-lived deposit token generated only after artist acceptance.
2. Keep tenant and booking resolution database-backed; production local fallback is disabled.
3. Verify amount/currency against persisted `Deposit.policySnapshot`.
4. Create Stripe Checkout Session or PaymentIntent using test/live credentials (staged environment only, evidence pending).
5. Persist provider session ID and provider idempotency result before returning redirect URL.
6. Rate-limit and monitor abuse.

### `POST /api/webhooks/stripe`

Status: **DB-first replay/audit reconciliation boundary with non-production local fallback; live Stripe proof remains evidence-gated**.

Purpose:
- Reserve the webhook endpoint boundary.
- Ensure the raw body is read before future signature verification.
- Interpret event type through `interpretStripeWebhook` for reconciliation planning.

Current implementation:
- Reads `request.text()`.
- Requires the `Stripe-Signature` header.
- Parses JSON only to inspect event type.
- Returns `200` with interpreted stripe event metadata and persisted local webhook intake.

Production requirements:
1. Use the Stripe SDK webhook helper with raw request body, signature header, and `STRIPE_WEBHOOK_SECRET`.
2. Reject invalid signatures before any reconciliation.
3. Fetch/verify Stripe objects when needed.
4. Enforce idempotency by event ID and provider object ID.
5. Reconcile `Deposit`, `Payment`, `Refund`, `BookingStateEvent`, and `PaymentAuditLog` records tenant-scoped.
6. Redact provider metadata and never log card data or full client notes.

### Payment response guidance

Payment route responses must keep the standard API envelope. Production routes must never return secret keys, webhook secrets, raw provider objects containing sensitive data, private file URLs, or medical/intake notes.

## Phase 8 calendar/travel route boundaries

### `GET /api/public/[tenantSlug]/calendar/[artistSlug]/travel.ics`

Implemented as a signed-feed route boundary. Non-production demo access requires the local signed feed token, rejects missing/invalid tokens with private no-store cache headers, emits access-log proof headers, and production fails closed until durable signed-token persistence, revocation checks, access-log persistence, generated Prisma/migration proof, client import smoke evidence, and secret-safe CI artifacts are captured. Tracked in `GAP-009` and `GAP-055`.

### `GET /api/public/[tenantSlug]/availability-preview`

Implemented as a DB-first read-only JSON preview route with non-production static demo fallback. It resolves tenant scope from Postgres when available, reads open/waitlist `AvailabilityWindow` rows, returns generated slots with raw window/slot/conflict objects and availability-window, tenant, artist, slot, and conflict identifiers suppressed (`rawAvailabilityWindowsEchoed=false`, `rawSlotObjectsEchoed=false`, `rawConflictObjectsEchoed=false`, `availabilityWindowIdEchoed=false`, `tenantIdEchoed=false`, `artistIdEchoed=false`, `slotIdEchoed=false`, `conflictIdsEchoed=false`, `conflictingBlockIdEchoed=false`, `internalPersistenceIdsEchoed=false`), and explicitly reports that holds, conflict writes, provider sync, and reservations are not executed. Production static fallback remains disabled. Transactional hold persistence, overlapping/race rejection, audit logs, tenant isolation tests, Google provider sync, and CI evidence remain tracked in `GAP-056` and `GAP-059`.

### Future Google Calendar provider routes

Provider-worker sync planning is wired at the dashboard boundary and returns no-store accepted/blocked responses with a safe redacted plan projection. OAuth connect/callback/disconnect, FreeBusy checks, appointment event insert/update/delete, full sync, incremental sync-token refresh, push channel renewal, and provider reconciliation workers still require Google Cloud credentials, encrypted token storage, and provider execution evidence. Tracked in `GAP-057` and `GAP-058`.

Local Google Calendar provider-result sanitization stores fixed redaction sentinels plus SHA-256 hashes for provider references, sync tokens, and provider identifier payload fields, with `rawProviderReferenceEchoed=false`, `rawNextSyncTokenEchoed=false`, and `rawProviderIdentifierEchoed=false` proof before local transaction capture. The local Google sync repository contract now also stores ciphertext-shaped refresh-token records with key metadata, digest proof, and `rawRefreshTokenStored=false`/`rawRefreshTokenEchoed=false`; live OAuth, real KMS/provider-backed encrypted token persistence, provider execution, push, and CI evidence remain gated.

Package Google Calendar sync write plans keep raw provider event/sync/push identifiers only on operational provider-event, sync-state, and push-channel writes. `CalendarAuditLog` plan payloads store SHA-256 identifier hashes plus `rawProviderEventIdStored=false`, `rawSyncTokenStored=false`, `rawPushChannelIdStored=false`, and `rawPushResourceIdStored=false` proof flags.


## Phase 9 notification/messaging route boundaries

### `POST /api/messages/privacy`

Status: **Local privacy workflow contract with production fail-closed guard**.

Dashboard messaging privacy planning is RBAC/tenant scoped and returns no-store responses with a safe plan projection. Raw message bodies, attachment URLs, thread/message IDs, actor IDs, tenant IDs, internal persistence IDs, and idempotency keys are not echoed in POST responses, and the GET contract now projects bundled demo plans through the same safe response builder with `rawContractPlansEchoed=false`, `tenantIdEchoed=false`, and `internalPersistenceIdsEchoed=false` instead of returning raw plan bodies, attachment URLs, IDs, actor IDs, tenant IDs, internal persistence IDs, or idempotency keys. Durable MessagePrivacyEvent/MessageAuditLog/IdempotencyKey persistence, retention/export/delete workflow execution, moderation/rate-limit integration, secure attachment authorization integration tests, CI, and secret-safe artifacts remain evidence-gated under `GAP-068`.

### `GET /api/public/[tenantSlug]/notification-previews`

Status: **Scaffolded static preview route with safe delivery-plan response projection**.

Purpose:
- Return rendered demo templates and consent-aware delivery plans from `@inkroute/notifications`.
- Project delivery-plan previews through an allowlisted response that omits raw contact fields, raw destinations, masked destinations, and raw consent snapshots while reporting response-level proof flags.
- Static and production fail-closed responses also report `tenantIdEchoed=false` and `internalPersistenceIdsEchoed=false`; provider-backed templates, consent persistence, durable queue writes, sandbox/device sends, and CI remain gated.
- `GET /api/templates` returns coded template metadata plus tenant-scoped queue/delivery summaries; persisted notification bodies, destination hashes, provider message IDs, and provider errors are not selected from the database, and response fields use fixed redaction sentinels with no-store caching and no tenant/notification/delivery/client/booking/appointment/audit/internal persistence ID echoes.
- Demonstrate email, SMS, push, and in-app routing decisions without sending anything.

Production requirements:
1. Replace static demo context with tenant-specific template settings.
2. Protect admin-only preview features behind dashboard auth.
3. Never expose private client destinations or provider secrets.
4. Preserve audit/redaction rules for previews.
5. Keep provider dispatch, durable queue writes, and live sends evidence-gated until provider-backed execution proof exists.

### `POST /api/public/[tenantSlug]/messages`

Status: **DB-first booking-context persistence with non-production local fallback; provider execution remains evidence-gated.**

Purpose:
- Accept public-safe client message/contact submissions.
- Validate minimal subject/body shape and return a redacted draft preview.
- Persist `MessageThread`, inbound `Message`, `Notification`, `NotificationDelivery`, `NotificationProviderHandoff`, `IdempotencyKey`, and `AuditLog` rows when a tenant-scoped `bookingRequestId` resolves to an existing client.
- Keep anonymous/contact-only local runtime fallback outside production until authenticated client reply tokens or another tenant-safe identity boundary exists.

Production requirements:
1. Resolve tenant safely by domain or slug.
2. Rate-limit, spam-protect, and optionally CAPTCHA public submissions.
3. Require an existing booking/client context, authenticated client identity, or short-lived reply token before DB persistence.
4. Route notifications to the artist/team through consent-aware queueing.
5. Execute provider workers and webhook replay/suppression proof only after credentials and provider sandboxes are available.
6. Redact medical/payment/private URL content from logs and error responses.

### `POST /api/webhooks/email` and `POST /api/webhooks/sms`

Status: **Local runtime interpretation and persistence implemented; provider verification and delivery-log persistence remain production work**.

Purpose:
- Reserve provider callback URLs.
- Parse event types and preview normalized delivery states.
- Demonstrate inbound SMS STOP handling boundary.

Production requirements:
1. Verify provider signatures before trusting payloads.
2. Enforce replay protection and idempotent event handling.
3. Persist delivery status updates to `NotificationDelivery`.
4. Apply suppression changes immediately for bounces, complaints, unsubscribes, and SMS STOP.
5. Route inbound SMS/email into tenant-scoped message threads only after validation and redaction.

Tracked in `GAP-061` through `GAP-069`.

Local email/SMS provider send-result sanitizers now retain only a fixed provider-message redaction sentinel, SHA-256 proof hash, and `rawProviderMessageIdEchoed=false` in persisted local proof artifacts. Raw provider message IDs are still accepted only as provider/webhook inputs for reconciliation and must be backed by live signature/replay/integration evidence before production completion.

Email/SMS webhook response projections now suppress raw provider event IDs, provider message IDs, tenant slugs/IDs, local stored-webhook row IDs, inbound SMS bodies, and internal persistence IDs from local receipt plus route-level readiness/reconciliation summaries (`webhookIdEchoed=false`, `rawProviderEventIdEchoed=false`, `rawProviderMessageIdEchoed=false`, `tenantSlugEchoed=false`, `tenantIdEchoed=false`, `internalPersistenceIdsEchoed=false`) while the notification webhook persistence helper itself returns proof booleans instead of ProviderEvent/AuditLog/delivery/transition/suppression/idempotency IDs. Internal provider-event/idempotency/suppression correlation is preserved for persistence attempts. Provider-secret configured replay, sandbox, exactly-once, suppression/inbound integration, CI, and secret-safe artifact evidence remain gated under `GAP-066`.


## Phase 10 SEO route boundaries

### `GET /api/public/[tenantSlug]/seo-preview`

Status: **Scaffolded static preview route**.

Returns static Phase 10 SEO engine output: route inventory, sitemap plan, audit results, content briefs, internal-link suggestions, Search Console setup draft, image SEO fields, and revalidation plan. The dashboard image SEO pipeline route is RBAC/tenant scoped, stores ID-free idempotency proof with `internalPersistenceIdsStored=false`, and returns a safe plan projection that does not echo tenant IDs, portfolio item IDs, source object keys, derivative object keys, public URLs, blur placeholders, alt/caption text, raw derivative metadata, provider payloads, or FileAsset/PortfolioImage/audit/idempotency/internal persistence IDs. Static SEO preview output does not read tenant content from Postgres, enforce RBAC, mutate SEO records, submit sitemaps, or verify Search Console ownership. Tracked in `GAP-071` through `GAP-078`.

Image SEO pipeline artifact review now redacts tenant, PortfolioItem, FileAsset, PortfolioImage, audit, idempotency, bucket, source/derivative object key, signed URL, public URL, blur placeholder, raw derivative metadata, provider payload, credential, and contact fields before retained storage/CDN/Lighthouse evidence is accepted. This remains a local/static artifact-safety contract only; storage-backed transforms, private-original denial, CDN cache/load proof, Lighthouse audit, persisted FileAsset/PortfolioImage execution, and CI artifacts remain gated under `GAP-077`.

Image SEO pipeline artifact review also redacts secret-safe evidence labels, artifact paths, CDN/storage/Lighthouse URLs, run selectors, and data-URL placeholders even when they appear under neutral keys before retained image optimization evidence is accepted; storage provider/CDN/Lighthouse execution proof remains gated under `GAP-077`.

Structured-data crawl artifact review now redacts rendered route paths/URLs, tenant selectors, rendered HTML, JSON-LD scripts, schema graphs, canonical/sitemap/robots/noindex values, validation payloads, provider payloads, drafts, credentials, and contacts before retained crawl evidence is accepted. This remains a local/static artifact-safety contract only; rendered browser crawl, Rich Results-compatible validation, sitemap/canonical/noindex crawl, unsupported-schema review, closeout evidence, and CI artifacts remain gated under `GAP-073`.

SEO automated-test artifact review now redacts linked crawl/runtime artifact URLs, route/canonical URLs, rendered HTML, command output, CI URLs, provider payloads, failure summaries, traces, credentials, and contacts before retained gate evidence is accepted. This remains a local/static artifact-safety contract only; SEO package test/typecheck execution, route/static contract output, linked `GAP-073`/`GAP-076` evidence, CI evidence, and produced secret-safe artifacts remain gated under `GAP-078`.

Phase 10 SEO runtime/build artifact review now redacts Search Console site/query rows, rendered crawl/canonical/sitemap URLs, tenant selectors, build logs, command output, CI artifact URLs, provider payloads, credentials, contacts, and private rendered HTML before retained runtime/build evidence is accepted. This remains a local/static artifact-safety contract only; web/dashboard builds, dashboard SEO browser smoke, rendered crawls, Search Console provider execution, CI evidence, and produced secret-safe artifacts remain gated under `GAP-076`.

Canonical/domain artifact review now redacts tenant hosts, canonical/redirect/sitemap URLs, noindex/route paths, rendered HTML, crawl payloads, DNS/domain verification values, provider payloads, credentials, and contacts before retained deployment-domain evidence is accepted. This remains a local/static artifact-safety contract only; custom-domain route tests, deployment-domain proof, rendered sitemap/canonical/noindex crawls, duplicate-canonical runtime proof, seeded DB repository execution, and CI artifacts remain gated under `GAP-072`.

Accessibility/visual artifact review now redacts route URLs, DOM/HTML snapshots, axe node targets, Lighthouse trace URLs, visual baseline/diff/screenshot paths, CI URLs, run IDs, commit SHAs, manual QA transcripts, screen-reader notes, credentials, contacts, and stack traces before retained accessibility/visual evidence is accepted. This remains a local/static artifact-safety contract only; browser/device/manual accessibility runs, visual diff review, CI evidence, regression triage, and provider-backed run persistence remain gated under `GAP-109`.

Public web launch artifact review now redacts rendered HTML, JSON-LD/canonical snapshots, route/legal review notes, media derivative and placeholder-asset evidence, screenshots/artifact URLs, provider/database details, tenant/customer/client identifiers, credentials, and contacts before retained launch evidence is accepted. This remains a local/static artifact-safety contract only; web build/test/smoke, Playwright, axe/Lighthouse, provider-backed public routes, real media derivatives, runtime SEO validation, legal review, CI evidence, and provider-backed run persistence remain gated under `GAP-006`.

Public content artifact review now redacts rendered HTML, API JSON/private review bodies, waitlist/FAQ request bodies, cache/revalidation tags, tenant/domain selectors, file/portfolio/travel/booking/attribution identifiers, provider/database details, credentials, contacts, and raw payloads before retained public-content evidence is accepted. This remains a local/static artifact-safety contract only; persisted tenant/domain resolver proof, DB/CMS seed proof, durable FAQ/waitlist persistence, rendered/API redaction proof, cache revalidation, browser smoke, CI evidence, and provider-backed run persistence remain gated under `GAP-026`.

Web build/runtime verification artifact review now redacts build logs, command/browser-smoke output, route paths, rendered HTML, stack traces, tenant/domain selectors, Prisma/database/session/env fields, credentials, contacts, medical/payment fields, and private payloads before retained build evidence is accepted. This remains a local/static artifact-safety contract only; Prisma generation, web typecheck/build, DB-backed route smoke, browser smoke, CI evidence, and provider-backed run persistence remain gated under `GAP-027`.

Dashboard Search Console operations are RBAC/tenant scoped, audit DB-backed requests when available, store idempotency proof flags with `rawIdempotencyKeyStored=false`/`internalPersistenceIdsStored=false` instead of raw keys in audit metadata, and return a safe operation-plan projection with tenant-match/audit-presence proof that does not echo tenant IDs, audit IDs, raw site URLs, sitemap URLs, step summaries, provider payloads, internal persistence IDs, or idempotency keys. Live provider execution remains credential-gated under `GAP-075`.

Dashboard notification queue writes persist tenant-scoped notification, delivery, provider-handoff, idempotency, and audit rows when DB-backed auth is available. Provider handoff correlation now stores a SHA-256 idempotency fingerprint, and audit metadata stores idempotency proof flags with `rawIdempotencyKeyStored=false`/`internalPersistenceIdsStored=false` instead of raw queue keys or internal row IDs; provider dispatch remains deferred/evidence-gated under `GAP-038`/`GAP-010`.

Dashboard notification scheduler GET responses expose runtime/readiness and required repository methods through safe schedule/process/retry/cancel/dead-letter plan summaries only; bundled demo scheduler plans are projected with `rawContractPlansEchoed=false`, `rawScheduledJobsEchoed=false`, `rawWritePayloadsEchoed=false`, `rawJobIdEchoed=false`, `rawAppointmentIdEchoed=false`, `rawBookingRequestIdEchoed=false`, `rawActorIdEchoed=false`, `rawCancellationReasonEchoed=false`, and `rawIdempotencyKeyEchoed=false` instead of returning raw demo job IDs, appointment/booking IDs, actor IDs, cancellation reasons, scheduled-job payloads, write payloads, or idempotency keys.
Dashboard notification scheduler GET/POST responses now also suppress tenant and internal persistence identifiers (`tenantIdEchoed=false`, `internalPersistenceIdsEchoed=false`) on contract, local-plan, DB success/replay, conflict, fail-closed, and database-unavailable branches while preserving counts, idempotency replay proof, and repository-required status; worker execution, Postgres due-job concurrency, retry/dead-letter, cancellation, provider dispatch, route tests, and CI remain gated under `GAP-065`/`GAP-066`.

Mobile booking lifecycle and travel publish local contracts continue to require tenant, request, entity, bearer auth, and `Idempotency-Key` headers/inputs for replay safety, but their plan metadata now returns required/no-echo proof flags instead of echoing raw authorization headers, access tokens, tenant IDs, booking IDs, travel-schedule IDs, request IDs, URLs, bodies, or offline/mobile idempotency keys. Provider token exchange, seeded API smoke, offline replay, and device/CI evidence remain gated under `GAP-043`.

Mobile push registration previews now expose a safe registration receipt with token persistence booleans and `rawExpoPushTokenEchoed=false`, `rawNativeDeviceTokenEchoed=false`, `rawAccessTokenEchoed=false`, `tenantIdEchoed=false`, `userIdEchoed=false`, `deviceIdEchoed=false`, and `internalPersistenceIdsEchoed=false` instead of echoing Expo/native/access tokens or device/user scope identifiers. Expo credentials, durable worker execution, generated Prisma client/migration proof, device QA, and CI remain gated under `GAP-063`.

Dashboard booking lifecycle success/replay and fallback responses return safe booking/state-event/transition receipts with `tenantIdEchoed=false`, `bookingRequestIdEchoed=false`, `eventIdEchoed=false`, and `rawTransitionEchoed=false` instead of echoing raw tenant, selected `BookingRequest`/`BookingStateEvent` rows, or transition internals. Provider rollback/execution and tenant-isolated integration evidence remain gated under `GAP-038`/`GAP-037`.

Mobile crash fallback payloads and GitHub issue automation artifacts now keep row-linkage evidence as proof flags (`rawReportIdEchoed=false`, `internalPersistenceIdsStored=false`) instead of copying local report, audit, or actor row IDs into metadata. GitHub issue automation responses return safe automation/runtime/dispatch projections with raw plan, create-request, dispatch response, issue URL, issue number, repository, provider token, audit ID, and internal persistence ID echoes suppressed. Provider crash capture, GitHub dispatch, approval workflow execution, and CI evidence remain gated under `GAP-046`/`GAP-081`/`GAP-083`/`GAP-085`.

Alert escalation responses now expose safe report and sanitized-payload previews with `rawReportEchoed=false`, `reportIdEchoed=false`, `fingerprintEchoed=false`, `rawPayloadEchoed=false`, `rawSanitizedPayloadEchoed=false`, and `internalPersistenceIdsEchoed=false` instead of returning local observability report IDs, fingerprints, or raw payload objects, while durable AlertDelivery/AuditLog persistence remains the internal linkage contract. Provider delivery, acknowledgement callbacks, worker execution, and CI evidence remain gated under `GAP-083`.

Stripe Checkout draft metadata now stores tenant/booking persistence proof flags with `rawTenantIdStored=false`, `rawBookingRequestIdStored=false`, and `internalPersistenceIdsStored=false` instead of duplicating tenant or booking identifiers into provider metadata. The provider idempotency key remains the replay control; live Checkout/provider transcript proof remains gated under `GAP-004`/`GAP-049`.

Local/mock Stripe Checkout session IDs now use short deterministic fingerprints instead of embedding the raw tenant/booking/idempotency scope or local deposit IDs in `providerSessionId` or checkout URLs, and the Stripe browser response contract returns provider redirect validation/no-echo proof rather than serializing checkout URLs or provider session IDs while live provider execution remains gated. Local signed-upload fallback URLs likewise use opaque mock handles instead of tenant slug/local upload ID path segments. These are local no-echo hardening measures only; live Stripe Checkout/storage-provider transcript proof remains gated under `GAP-004`/`GAP-005`/`GAP-049`.

Package payment lifecycle and operation write plans now keep raw provider identifiers only on operational payment/refund state writes. `PaymentAuditLog` plan payloads store SHA-256 provider identifier hashes plus `rawProviderSessionIdStored=false`, `rawProviderPaymentIntentIdStored=false`, and `rawProviderChargeIdStored=false` proof flags. Live Stripe execution, reconciliation, and persisted database evidence remain gated under `GAP-004`/`GAP-024`/`GAP-052`.

Payment persistence artifact review now redacts payment/deposit/refund/dispute lifecycle evidence, idempotency and transaction payloads, Prisma/Postgres logs, audit rows, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/booking labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained payment persistence evidence is accepted. This remains a local/static artifact-safety contract only; seeded Postgres integration, DB-backed replay/idempotency tests, cross-tenant mutation denial, dashboard repository route/action tests, CI proof, and redacted artifact capture remain gated under `GAP-051`.

Dashboard payment operation provider results are re-sanitized before persistence/return: raw provider references and payload identifier fields are replaced with fixed redaction sentinels plus SHA-256 hashes and `rawProviderReferenceEchoed=false`/`rawProviderIdentifierEchoed=false` proof. Live Stripe refund/dispute execution and receipt/export provider evidence remain gated under `GAP-052`.

Payment operations artifact review now redacts Stripe refund/dispute evidence, receipt/export/accounting/tax payloads, provider result payloads, idempotency/audit fields, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/payment labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained payment operations evidence is accepted. This remains a local/static artifact-safety contract only; live Stripe refund execution, no-show audit persistence, dispute provider sync, delivered receipt proof, accounting/tax sign-off, dashboard E2E artifacts, CI proof, and redacted artifact capture remain gated under `GAP-052`.

Live Stripe payment artifact review now redacts provider request/response transcripts, webhook bodies/signatures, Checkout/PaymentIntent/refund/dispute identifiers, reconciliation/idempotency/replay traces, receipt/audit payloads, command output, CI/workflow/run selectors, artifact paths, database/provider fields, tenant/client/customer/payment labels, contacts, credentials, tokens, and authorization headers before retained payment evidence is accepted. This remains a local/static artifact-safety contract only; Stripe secret/webhook configuration proof, provider Checkout/PaymentIntent lifecycle execution, webhook CLI replay, refund/dispute execution, booking-to-paid E2E, CI proof, and provider-backed `LiveStripePaymentsRun` persistence remain gated under `GAP-004`.

### `GET /api/public/[tenantSlug]/sitemap-preview`

Status: **Scaffolded static preview route**.

Returns static sitemap-plan JSON for inspection. Production sitemap generation must use tenant-domain canonical resolution, SeoCityPage/SeoStylePage/SeoRedirect records, draft/archive/noindex filtering, cache revalidation, and tests. Tracked in `GAP-072`, `GAP-076`, and `GAP-078`.

## Phase 11 observability contract notes

Observability endpoints are partially production-oriented. Alert escalation responses return persistence proof flags without AlertDelivery, AuditLog, or internal persistence ID echoes, and AlertDelivery audit metadata stores `reportMatched`/`alertDeliveryRecorded` proof with `auditLogged=false`/`auditLogIdEchoed=false` on unresolved-tenant paths and `internalPersistenceIdsStored=false` instead of ErrorReport/AlertDelivery IDs. They build sanitized drafts through `@inkroute/observability`, apply rate limiting and tenant resolution, persist redacted `ErrorReport` rows on DB-available paths, and fall back to local runtime only where explicitly allowed outside production. Public error-report success responses now use allowlisted redacted receipts plus `rawPayloadEchoed=false`/`rawMessageEchoed=false`/`rawMetadataEchoed=false`/`rawStackEchoed=false` and internal-ID echo denial proof flags instead of returning raw local persistence objects or database audit/abuse/report IDs. Sentry webhook success responses now return safe report/workflow/issue-draft and sanitized-payload summaries with raw report, workflow, issue draft, provider payload, ProviderWebhookDelivery ID, AuditLog ID, matched ErrorReport ID, and internal persistence ID echoes suppressed; the reconciliation helper returns boolean persistence proof instead of ID-shaped nullable fields. Dashboard error-report read/write APIs are present with tenant/RBAC guards, recursive metadata redaction on reads, no-store responses, and AuditLog writes; provider signature verification, alert/escalation provider configuration, live incident integrations, and CI evidence remain pending.

Error-report responses must never include raw stack traces, client PII, medical notes, consent signatures, payment data, cookies, authorization headers, or provider tokens. Future persistent records should store redacted metadata only, plus `stackHash`, `fingerprint`, severity, source, route, release, runtime, and environment.

## Phase 12 release/feature-flag route boundaries

Dashboard release and feature-flag routes use the shared dashboard actor guard plus persisted TenantMember lookup helper for write/read authorization. The membership helper independently fail-closes `local-fallback` actor contexts in production with `AUTH_REQUIRED`, and release/feature-flag membership, concurrency, and orchestration projections return proof flags (`tenantIdEchoed=false`, `actorUserIdEchoed=false`, `membershipIdEchoed=false`, `customRoleIdEchoed=false`, `recordIdEchoed=false`) instead of raw tenant, actor, TenantMember, CustomRole, ReleaseRecord, or FeatureFlag identifiers, so route-level fallback behavior cannot bypass provider-backed session and persisted membership requirements.

### `GET /api/releases`

Dashboard-only route returning tenant-scoped release candidates, rollback drafts, and feature-flag decision context.

Current status: authenticated (`release:read`) and tenant-scoped reads with DB-backed persistence and fallback behavior when DB is unavailable.

Required production pipeline:

1. authenticate dashboard user
2. resolve tenant membership and role
3. authorize release-read permission
4. fetch `ReleaseRecord`, `FeatureFlag`, and deployment evidence from Postgres
5. redact provider secrets and internal incident details
6. return tenant-scoped release control-plane payload

### `POST /api/releases`

Current status: validates request schema, persists idempotency-backed `ReleaseRecord` and `AuditLog` metadata on DB-available paths, and falls back to local response without durable writes when DB is unavailable outside production.

Future production behavior:

- create draft `ReleaseRecord`
- attach release notes, commit SHA, artifact metadata, and migration plan
- evaluate release gates
- write audit log
- optionally trigger preview CI/CD workflow
- never deploy production without protected-environment approval

### `PATCH /api/releases`

Current status: validates rollback intent schema, enforces `release:write` tenant membership, records idempotency-backed `AuditLog` metadata on DB-available paths, and returns a rollback plan with `providerRollbackExecuted=false`, `deploymentJobTriggered=false`, `protectedEnvironmentTouched=false`, `sourceReleaseRecordIdEchoed=false`, and `providerRollbackPayloadEchoed=false`.

Dashboard release create/read responses now project release candidates and release-record summaries through no-echo receipts (`tenantIdEchoed=false`, `releaseCandidateIdEchoed=false`, `releaseRecordIdEchoed=false`, `commitShaEchoed=false`, `internalPersistenceIdsEchoed=false`) on DB and local-fallback paths while preserving version/channel/gate/health status and internal audit/workflow linkage; protected-environment provider execution, rollback transcript, CI artifacts, and tenant-isolated evidence remain gated under `GAP-015`/`GAP-088`/`GAP-122`/`GAP-125`.

Release launch-control artifact review now redacts GitHub/Vercel/EAS/Expo/Sentry provider fields, release/feature-flag/tenant/user/actor selectors, protected-environment approvals, workflow/job/run/CI logs, commit SHAs, deployment/provenance URLs, migration dry-run output, rollback/incident transcripts, EAS rollout and kill-switch payloads, release-health/provider-route evidence, RBAC/concurrency/audit/idempotency proof, artifact paths, database/DSN fields, stacks/errors, contacts, credentials, tokens, and authorization headers before retained release launch evidence is accepted. This remains a local/static artifact-safety contract only; provider-backed ReleaseRecord/FeatureFlag persistence, protected environments, signed jobs, CI required checks, preview/prod approval dry runs, migration gates, incident rollback, EAS governance, provider route tests, live CI artifacts, and provider-backed `ReleaseLaunchControlRun` persistence remain gated under `GAP-015`.

Environment contract note: `.env.example` and `ENVIRONMENT_VARIABLES.md` enumerate runtime-read and credential-gated values used by booking anti-bot verification, encryption key labels, webhook aliases, alert escalation, GitHub issue automation, release governance dispatch, OpenTelemetry sampling, EAS/mobile runtime labels, and incident linkage. Blank/example values are not production evidence; secrets must be provisioned only in provider secret stores and redacted from runtime artifacts.

Release persistence/RBAC artifact review now recursively redacts release-control selectors before persistence, including tenant, actor, membership, custom role, release record, feature flag, workflow-run, workflow URL, commit SHA, audit, and idempotency identifiers. This is a local/static artifact-safety contract only; DB-backed concurrency, tenant-isolation, rendered workflow, protected-environment execution, and CI artifact evidence remain gated under `GAP-088`.

Future production behavior: execute protected-environment provider rollback only after CI/deployment credentials, approval evidence, rollback job transcripts, and post-rollback release-health proof are attached.

Public release-health responses now suppress tenant, runtime-context tenant, release candidate, release-record, and commit identifiers (`tenantIdEchoed=false`, `runtimeContextTenantIdEchoed=false`, `releaseCandidateIdEchoed=false`, `releaseRecordIdEchoed=false`, `commitShaEchoed=false`, `internalPersistenceIdsEchoed=false`) while preserving version/channel/gate/health status; provider-backed release/feature-flag integration tests, protected-environment proof, rollback transcript, and CI artifacts remain gated under `GAP-015`/`GAP-087`/`GAP-090`/`GAP-094`.

Release runtime verification artifact review now redacts release-health route URLs, dashboard release/feature-flag route payloads, build/typecheck/mobile logs, release-governance workflow dry-run output, GitHub Actions job/run/check selectors, CI artifact paths, commit selectors, tenant/release/feature selectors, raw payloads, stacks/errors, contacts, credentials, tokens, and authorization headers before retained release-runtime evidence is accepted. This remains a local/static artifact-safety contract only; package checks, route smokes, web/dashboard/mobile builds, workflow dry-run, GitHub Actions proof, CI artifact attachment, and provider-backed evidence remain gated under `GAP-087`.

Release automated coverage artifact review now redacts Playwright dashboard smoke URLs/traces/screenshots/videos, provider-backed release/feature-flag route payloads, Expo/EAS/device identifiers, workflow and CI run selectors, real secret/environment evidence, artifact paths, commit selectors, tenant/release/feature selectors, raw payloads, stacks/errors/logs, contacts, credentials, tokens, and authorization headers before retained release coverage evidence is accepted. This remains a local/static artifact-safety contract only; live Playwright, Expo/device, provider route integration, GitHub Actions workflow execution, real protected-environment secret proof, dashboard typecheck, CI artifacts, and provider-backed evidence remain gated under `GAP-094`.

### `GET /api/feature-flags`

Current status: resolves persisted global + tenant flag records from DB and evaluates tenant-scoped decisions before returning safe decision snapshots with tenant-match proof, redacted cache-key scope, and no raw tenant ID echo.

Future production behavior:

- resolve flags from persisted tenant/global/environment records
- return only safe flag fields to clients
- keep provider kill switches server-side
- include cache/invalidation metadata

### `POST /api/feature-flags`

Current status: validates mutation payloads, persists feature-flag updates with audit metadata on DB-available paths, returns tenant-scoped no-echo mutation receipts with redacted invalidation targets, and blocks provider-credentialed enables when required credentials are missing.

Future production behavior:

- require owner/admin RBAC
- validate flag key/scope/environment
- apply optimistic concurrency or versioning
- write audit log
- invalidate flag caches
- prevent enabling credential-gated providers without verification evidence

### `GET /api/public/[tenantSlug]/release-health`

Current status: returns release health from tenant-scoped persisted `ReleaseRecord` and `FeatureFlag` rows when DB is available, with a safe fallback snapshot otherwise.

Feature-flag decision contexts use a sanitized allowlist of public headers (`x-inkroute-tenant-id`, `x-inkroute-user-id`, `x-inkroute-role`) instead of passing full request headers through the route.

Feature-flag runtime artifact review now redacts request-derived tenant/user/role contexts, rollout buckets and stable identifiers, provider kill-switch decisions, cache keys and invalidation metadata, release-health route payloads and headers, dashboard/mobile provider previews, live-proof logs, DB-backed rollout evidence, CI/workflow/run selectors, artifact paths, raw payloads, provider/database fields, stacks/errors, contacts, credentials, tokens, and authorization headers before retained feature-flag evidence is accepted. This remains a local/static artifact-safety contract only; provider-worker kill-switch drills, DB-backed rollout bucket proof, dashboard/mobile typecheck evidence, live rollout proof, CI artifacts, and provider-backed evidence remain gated under `GAP-090`.

Future production behavior:

- expose only public-safe app version/channel and broad status
- never leak internal deployment URLs, incident details, flag rollout notes, provider secrets, tenant counts, or stack traces
- use cache headers appropriate for public status metadata


## Phase 13 security/privacy/trust route boundaries

### `GET /api/public/[tenantSlug]/upload-policy`

Status: **Scaffolded read-only preview**.

Returns tenant slug, upload policy drafts, public rate-limit drafts, security header drafts, safe scan-pipeline/private-storage access preview controls, and gap IDs. The scan preview does not echo the raw plan, raw validation object, sample filename, sample file signature, malware verdict, tenant ID, file asset ID, or derivative object key; the private-storage preview does not echo raw tenant IDs, subject IDs, object keys, bucket names, signed URLs, or raw plan objects. This route does not create signed upload URLs, authenticate users, scan files, or persist `FileAsset` rows.

### `POST /api/public/[tenantSlug]/secure-upload-intents`

Status: **DB-first booking-context upload intent persistence with non-production local fallback; provider URL minting remains gated**.

Expected JSON fields: `kind`, `filename`, `mimeType`, `sizeBytes`, optional `bookingRequestId`, and optional `declaredByAuthenticatedUser`. The route validates metadata with `@inkroute/security`, applies local rate limiting, resolves tenant scope from the database when available, and persists `FileAsset`, `SignedUrlGrant`, optional `ReferenceImage`, and redacted `AuditLog` rows when a tenant-scoped booking/client plus issuer context exists. DB-backed and local fallback responses now return safe receipts/proof flags without bucket, object key, signed upload URL field, signed URL hash, raw plan object, local draft, ReferenceImage/AuditLog row objects, or internal persistence ID echoes. Runtime readiness now credits only DB-scoped metadata rows and source-level no-echo proof; local runtime mock intent fallback remains available only outside production when DB context is unavailable. Production still must require an authenticated user or short-lived booking upload token for anonymous uploads, mint provider-signed URLs, verify magic bytes, strip metadata, scan/quarantine files, prove private bucket ACLs and cross-tenant denial, run integration tests, and capture CI/provider evidence.

### `POST /api/public/[tenantSlug]/privacy-requests`

Status: **DB-first public privacy intake with local fallback outside production**.

Expected JSON fields: `type`, `email`, optional `requesterName`, and optional `details`. The route rate-limits by tenant/client, resolves tenant scope from the database when available, persists `IdempotencyKey`, `PrivacyRequest`, and redacted `AuditLog` rows on DB-backed paths, stores only redacted requester metadata plus ID-free idempotency proof metadata, stores ID-free result evidence with `internalPersistenceIdsStored=false`, returns a safe receipt and draft projection without raw payload, redacted submission, requester hash, idempotency key, or internal persistence ID echoes, and uses local runtime fallback only outside production when the DB is unavailable. Production still requires identity verification, attorney-reviewed deadlines and language, export/delete/rectification workers, legal retention holds, notifications, integration tests, and CI evidence before GAP-098 can close.

### `GET /api/security/trust-status`

Status: **Dashboard shared-auth read-only trust boundary with production provider-session fail-close**.

Returns security posture summary, controls, tenant isolation fixtures, upload policies, rate-limit rules, CSRF plans, and header drafts through the shared dashboard actor/RBAC guard plus a trust-specific operator-role allowlist without echoing tenant, actor user, or internal persistence identifiers (`tenantIdEchoed=false`, `actorUserIdEchoed=false`, `internalPersistenceIdsEchoed=false`). Production still fails closed until provider-backed session, persisted tenant membership, audit-ready route evidence, and current security runtime artifacts exist.

### `POST /api/security/privacy-requests`

Status: **DB-backed dashboard intake route added for authenticated tenant actors**.

Validates dashboard privacy request intake, enforces tenant write permission, rate-limits by tenant/actor/IP, verifies optional client scope, persists `PrivacyRequest` plus redacted `AuditLog`, returns no-store workflow evidence with requester email redacted and not selected from DB response projection, and suppresses tenant ID, PrivacyRequest ID, actor user ID, AuditLog ID, requester email, raw payload, and internal persistence ID echoes (`tenantIdEchoed=false`, `privacyRequestIdEchoed=false`, `actorUserIdEchoed=false`, `auditIdEchoed=false`, `requesterEmailEchoed=false`, `rawPayloadEchoed=false`, `internalPersistenceIdsEchoed=false`) on DB, production fail-closed, and local demo response envelopes. Local in-memory fallback is disabled for production. Export/delete/rectification workers, private file deletion, retention tombstone execution, sanitized log/error capture, attorney/product approval, dashboard build/typecheck, route tests, and CI evidence remain required before GAP-040 can close.

Dashboard privacy runtime artifact review now redacts privacy request/export/delete/anonymize workflow payloads, private file and consent-signature references, medical/payment/SMS/message evidence, sanitized log/error bundles, legal/product approval payloads, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/booking/payment/message labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained dashboard privacy evidence is accepted. This remains a local/static artifact-safety contract only; export/delete/anonymize workflow integration, private storage deletion tests, sanitized runtime log/error evidence, attorney/product approval, dashboard typecheck/build, dashboard privacy route integration tests, CI proof, and redacted artifact capture remain gated under `GAP-040`.

Dashboard form list and detail reads now return key/version/type metadata with `formIdsEchoed=false`, `formIdEchoed=false`, `tenantIdEchoed=false`, `questionIdsEchoed=false`, `auditIdEchoed=false`, and `medicalAcknowledgmentIdsEchoed=false` across database, local-fallback, production fail-closed, not-found, and database-unavailable branches instead of raw tenant, IntakeForm/ConsentForm/question/acknowledgment/AuditLog row IDs; legal copy approval, signature workflows, private upload retention, route execution, and CI evidence remain gated under `GAP-013`/`GAP-040`.

Dashboard payment detail reads now return redacted provider/payment metadata with `paymentIdEchoed=false`, `tenantIdEchoed=false`, `bookingRequestIdEchoed=false`, `depositIdEchoed=false`, `refundIdsEchoed=false`, `auditIdEchoed=false`, and `paymentAuditIdEchoed=false` across database, local-fallback, production fail-closed, and database-unavailable branches, using tenant-match proof and linked/presence booleans instead of raw Payment/BookingRequest/Deposit/Refund/AuditLog/PaymentAuditLog row IDs; Stripe provider reconciliation, receipt/export evidence, route execution, and CI remain gated under `GAP-004`/`GAP-040`.

Availability persistence artifact review now redacts availability hold/race evidence, artist/calendar identifiers, idempotency/audit payloads, Prisma/Postgres transaction traces, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/booking labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained availability evidence is accepted. This remains a local/static artifact-safety contract only; Postgres mutation integration, concurrent hold race rejection, tenant isolation, seeded route execution, dashboard build, CI proof, and redacted artifact capture remain gated under `GAP-056`.

Google Calendar sync artifact review now redacts Google OAuth/refresh-token evidence, calendar/event/freebusy/sync/push-channel payloads, webhook/provider identifiers, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/artist labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained Google Calendar evidence is accepted. This remains a local/static artifact-safety contract only; live Google OAuth, encrypted token persistence, provider sync worker execution, FreeBusy/event/push smokes, tenant isolation, route tests, CI proof, and redacted artifact capture remain gated under `GAP-057`.

Timezone recurrence artifact review now redacts timezone/provider render payloads, Google/ICS import/render evidence, appointment/travel/recurrence identifiers, seeded persistence artifacts, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/artist labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained timezone recurrence evidence is accepted. This remains a local/static artifact-safety contract only; explicit timezone/date boundary execution proof, stored recurrence expansion, Google/ICS render smokes, seeded persistence-boundary evidence, CI proof, and redacted artifact capture remain gated under `GAP-058`.

Dashboard payment list reads now return redacted provider/payment metadata with `paymentIdsEchoed=false`, `tenantIdEchoed=false`, `bookingRequestIdsEchoed=false`, `depositIdsEchoed=false`, `refundIdsEchoed=false`, and `auditIdEchoed=false` across database, local-fallback, production fail-closed, and database-unavailable branches, using tenant-match proof and linked/presence booleans instead of raw Payment/BookingRequest/Deposit/Refund/AuditLog row IDs; Stripe provider reconciliation, receipt/export evidence, route execution, and CI remain gated under `GAP-004`/`GAP-040`.

Dashboard message thread detail reads now return redacted thread/message metadata with `threadIdEchoed=false`, `tenantIdEchoed=false`, `clientIdEchoed=false`, `bookingRequestIdEchoed=false`, `appointmentIdEchoed=false`, `messageIdsEchoed=false`, and `senderIdsEchoed=false` across database, local-fallback, production fail-closed, and database-unavailable branches, using tenant-match proof, linkage booleans, and sender type labels instead of raw tenant/thread/message/sender relationship IDs; provider send/replay, suppression reconciliation, route execution, and CI remain gated under `GAP-010`/`GAP-064`/`GAP-068`.

Dashboard message thread list reads now suppress thread, tenant, client, booking, appointment, latest-message, sender, audit, and internal persistence identifiers (`threadIdsEchoed=false`, `tenantIdEchoed=false`, `clientIdsEchoed=false`, `bookingRequestIdsEchoed=false`, `appointmentIdsEchoed=false`, `messageIdsEchoed=false`, `senderIdsEchoed=false`, `auditIdEchoed=false`, `internalPersistenceIdsEchoed=false`) across database, local-fallback, production fail-closed, and database-unavailable branches while preserving tenant-match proof, redacted thread summaries, linkage booleans, and sender type labels; provider send/replay, suppression reconciliation, route execution, and CI remain gated under `GAP-010`/`GAP-064`/`GAP-068`.

Dashboard booking detail reads now suppress `BookingRequest`, tenant, portfolio attribution, assignee, audit, and state-event row IDs (`bookingRequestIdEchoed=false`, `tenantIdEchoed=false`, `portfolioAttributionIdEchoed=false`, `assignedToUserIdEchoed=false`, `auditIdEchoed=false`, `stateEventIdsEchoed=false`) across database, local-fallback, production fail-closed, and database-unavailable branches while keeping tenant-match proof, redacted booking content, and timeline status metadata; seeded tenant/RBAC/redaction route evidence and CI remain gated under `GAP-037`.

Dashboard data-layer artifact review now redacts dashboard read route payloads, Prisma/repository query traces, seeded tenant fixtures, RBAC/redaction proof, sensitive-read AuditLog payloads, booking/client/payment/portfolio/travel/message row selectors, storage object keys, command output, CI/workflow/run selectors, artifact paths, provider/database fields and DSNs, tenant/member/client labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained dashboard data evidence is accepted. This remains a local/static artifact-safety contract only; seeded database route smoke, tenant-isolation/RBAC/redaction tests, sensitive-read AuditLog persistence, dashboard typecheck/build, no-store proof, CI proof, and secret-safe artifact capture remain gated under `GAP-037`.

Dashboard booking list reads now suppress BookingRequest, tenant, portfolio attribution, assignee, audit, and internal persistence identifiers (`bookingRequestIdsEchoed=false`, `tenantIdEchoed=false`, `portfolioAttributionIdsEchoed=false`, `assignedToUserIdsEchoed=false`, `auditIdEchoed=false`, `internalPersistenceIdsEchoed=false`) across database, local-fallback, production fail-closed, and database-unavailable branches while preserving tenant-match proof, redacted booking summary fields, and attribution/assignment presence booleans; seeded tenant/RBAC/redaction route evidence and CI remain gated under `GAP-037`.

Dashboard client detail reads now suppress Client, tenant, and related booking row IDs (`clientIdEchoed=false`, `tenantIdEchoed=false`, `relatedBookingIdsEchoed=false`) on database, local-fallback, production fail-closed, and database-unavailable branches while preserving tenant-match proof, redacted contact/medical/private-note sentinels, and aggregate booking/payment counts; seeded tenant/RBAC/redaction route evidence, export/delete workflows, and CI remain gated under `GAP-037`/`GAP-040`.

Dashboard client private-note writes now suppress tenant and Client identifiers (`tenantIdEchoed=false`, `clientIdEchoed=false`) in addition to audit, idempotency, raw note, and internal persistence identifiers on DB success/replay, idempotency-conflict, not-found, production/local fallback, and database-unavailable branches while returning tenant-match proof; tenant-isolated mutation, privacy workflow, dashboard build, and CI evidence remain gated under `GAP-038`/`GAP-040`.

Dashboard mutation artifact review now redacts mutation route request/response payloads, Prisma transaction traces, idempotency and AuditLog payloads, rollback/retry/operator-review transcripts, provider side-effect payloads, UI feedback-state traces, duplicate/conflict proof, command output, CI/workflow/run selectors, artifact paths, provider/database fields and DSNs, tenant/client/booking/payment/portfolio/travel/form/message labels, secret-safe evidence labels, contacts, credentials, tokens, and authorization headers before retained dashboard mutation evidence is accepted. This remains a local/static artifact-safety contract only; mutation route tests, Prisma transaction tests, tenant-isolation/RBAC/audit proof, provider rollback/retry integration, UI feedback-state tests, dashboard typecheck/build, CI proof, and secret-safe artifact capture remain gated under `GAP-038`.

Dashboard client list reads now suppress Client, tenant, related booking, audit, and internal persistence identifiers (`clientIdsEchoed=false`, `tenantIdEchoed=false`, `relatedBookingIdsEchoed=false`, `auditIdEchoed=false`, `internalPersistenceIdsEchoed=false`) across database, local-fallback, production fail-closed, and database-unavailable branches while preserving tenant-match proof, redacted contact fields, medical/private-note presence indicators, payment aggregates, and related-booking linkage booleans; seeded tenant/RBAC/redaction route evidence, export/delete workflows, and CI remain gated under `GAP-037`/`GAP-040`.

Dashboard portfolio detail reads now suppress PortfolioItem, tenant, attributed booking, PortfolioImage, FileAsset, audit, and internal row IDs (`portfolioItemIdEchoed=false`, `tenantIdEchoed=false`, `attributedBookingIdsEchoed=false`, `portfolioImageIdsEchoed=false`, `fileAssetIdsEchoed=false`, `auditIdEchoed=false`, `internalPersistenceIdsEchoed=false`) across database, local-fallback, production fail-closed, and database-unavailable branches while preserving tenant-match proof, public-safe image metadata, and storage-key redaction; provider storage execution, derivative/scan proof, route evidence, and CI remain gated under `GAP-005`/`GAP-037`/`GAP-040`.

Dashboard portfolio list reads now suppress PortfolioItem, tenant, attributed booking, PortfolioImage, FileAsset, audit, and internal persistence identifiers (`portfolioItemIdsEchoed=false`, `tenantIdEchoed=false`, `attributedBookingIdsEchoed=false`, `portfolioImageIdsEchoed=false`, `fileAssetIdsEchoed=false`, `auditIdEchoed=false`, `internalPersistenceIdsEchoed=false`) across database, local-fallback, production fail-closed, and database-unavailable branches while preserving tenant-match proof, public-safe image metadata, attribution/image counts, and file-asset linkage booleans; provider storage execution, derivative/scan proof, route evidence, and CI remain gated under `GAP-005`/`GAP-037`/`GAP-040`.

Dashboard travel detail reads now suppress TravelSchedule, tenant, artist, TravelCity, AvailabilityWindow, BookingRequest, and Appointment row IDs (`travelScheduleIdEchoed=false`, `tenantIdEchoed=false`, `artistIdEchoed=false`, `travelCityIdEchoed=false`, `availabilityWindowIdsEchoed=false`, `bookingRequestIdsEchoed=false`, `appointmentIdsEchoed=false`) across database, local-fallback, production fail-closed, and database-unavailable responses while preserving tenant-match proof, city, availability, booking, and appointment counts plus redacted internal-note/client context; provider sync, public cache/SEO, route evidence, and CI remain gated under `GAP-037`/`GAP-046`/`GAP-047`.

Dashboard intake and consent form creation receipts now suppress tenant, form, existing-form, audit, idempotency-key, raw idempotency-key, raw consent body, and internal persistence identifiers across success/replay, duplicate, production fail-closed, local-fallback, and database-unavailable responses while preserving tenant-match proof and safe form metadata; question authoring, response persistence, legal approval, signature/file workflows, medical acknowledgments, privacy review, integration tests, dashboard build, and CI remain gated under `GAP-038`/`GAP-040`.

Dashboard availability creation responses now suppress tenant, AvailabilityWindow, conflicting AvailabilityWindow, artist, TravelCity, TravelSchedule, AuditLog, idempotency-key, raw idempotency-key, and internal persistence identifiers across success/replay, conflict, production fail-closed, local-fallback, and database-unavailable branches while preserving tenant-match proof, conflict timing/status metadata, persisted overlap guard proof, and serializable transaction configuration; concurrent hold race proof, seeded tenant-isolated route tests, dashboard build, and CI remain gated under `GAP-038`/`GAP-056`.

Dashboard calendar feed reads now suppress tenant, CalendarConnection, CalendarEvent, AvailabilityWindow, artist, appointment, travel-city, travel-schedule, audit, and internal persistence identifiers (`tenantIdEchoed=false`, `calendarConnectionIdsEchoed=false`, `calendarEventIdsEchoed=false`, `availabilityWindowIdsEchoed=false`, `artistIdsEchoed=false`, `appointmentIdsEchoed=false`, `travelCityIdsEchoed=false`, `travelScheduleIdsEchoed=false`, `internalPersistenceIdsEchoed=false`) on DB and local-fallback paths while preserving provider redaction and linked/presence booleans; Google OAuth/sync/provider execution, route evidence, and CI remain gated under `GAP-009`/`GAP-057`/`GAP-058`.
Dashboard timezone QA diagnostic responses now suppress tenant and internal persistence identifiers (`tenantIdEchoed=false`, `internalPersistenceIdsEchoed=false`) on local diagnostic and production fail-closed paths while preserving boundary validation results, QA plan, readiness blockers, and no-store headers; Temporal/date-library proof, persistence-boundary validation, Google/ICS render smokes, seeded persistence evidence, and CI remain gated under `GAP-058`/`GAP-059`.
Dashboard Google Calendar sync planning responses now return a safe provider-worker plan projection with raw write payloads, idempotency keys, provider event IDs, sync tokens, push channel/resource IDs, tenant IDs, and internal persistence IDs suppressed (`writePayloadsEchoed=false`, `rawIdempotencyKeyEchoed=false`, `providerEventIdEchoed=false`, `syncTokenEchoed=false`, `pushChannelIdEchoed=false`, `pushResourceIdEchoed=false`, `tenantIdEchoed=false`, `internalPersistenceIdsEchoed=false`) while preserving provider-worker-required status and readiness blockers; Google OAuth, encrypted token persistence, provider worker execution, FreeBusy/event/push smokes, tenant isolation, and CI remain gated under `GAP-057`.

Dashboard Search Console status responses now expose `siteUrlConfigured` and credential/readiness booleans instead of echoing the raw configured Search Console site URL, credentials, or private key material (`rawSiteUrlEchoed=false`, `rawCredentialsEchoed=false`, `rawPrivateKeyEchoed=false`); live provider proof, background jobs, imported-row persistence, and CI remain gated under `GAP-075`.

Search Console artifact review now redacts tenant/run selectors, raw site/sitemap URLs, idempotency keys, imported-row/query/page values, credentials, provider payloads, Google private key material, contact values, and authorization headers before retained provider evidence is accepted; verified property proof, sitemap smoke, import persistence, background jobs, provider sandbox tests, and CI remain gated under `GAP-075`.

SEO publication artifact review now redacts tenant, actor, entity, related-content, idempotency, and revalidation-tag selectors in addition to provider payloads, Search Console values, draft copy/body fields, contact values, and credentials before retained publication evidence is accepted; seeded Prisma execution, tenant isolation, dashboard/browser publication flows, revalidation execution, and CI remain gated under `GAP-071`.
Dashboard calendar hold responses now return safe plan/success/conflict/idempotency projections with raw write payloads, idempotency keys, booking request IDs, availability-window/hold IDs, actor IDs, tenant IDs, and internal persistence IDs suppressed (`writePayloadsEchoed=false`, `rawIdempotencyKeyEchoed=false`, `bookingRequestIdEchoed=false`, `availabilityWindowIdEchoed=false`, `holdIdEchoed=false`, `actorIdEchoed=false`, `tenantIdEchoed=false`, `internalPersistenceIdsEchoed=false`) while preserving idempotency replay and repository-required status; seeded race-condition, tenant-isolation, Postgres route execution, and CI remain gated under `GAP-056`.

Dashboard travel list reads now use the same no-echo boundary for tenant, TravelSchedule, artist, TravelCity, AvailabilityWindow, BookingRequest, Appointment, audit, and internal persistence identifiers across database, local-fallback, production fail-closed, and database-unavailable branches while preserving tenant-match proof, city/public availability summaries, and linked/count booleans; provider sync, public cache/SEO, route evidence, and CI remain gated under `GAP-037`/`GAP-046`/`GAP-047`.

Public message intake receipts now suppress tenant, message, booking-request, body-preview, raw body, and internal persistence identifiers on DB and local fallback paths (`tenantIdEchoed=false`, `messageIdEchoed=false`, `bookingRequestIdEchoed=false`, `bodyPreviewEchoed=false`, `rawMessageBodyEchoed=false`, `internalPersistenceIdsEchoed=false`) while preserving tenant slug, queued status, linked-booking proof, and provider-handoff state; provider worker execution, replay/suppression proof, tenant-isolated integration tests, and CI remain gated under `GAP-010`/`GAP-064`/`GAP-066`.

Public error-report receipts now suppress tenant, local/database report identifiers, and stack hashes (`tenantIdEchoed=false`, `errorReportIdEchoed=false`, `stackHashEchoed=false`, `internalPersistenceIdsEchoed=false`) on DB and local fallback paths while preserving tenant slug, correlation headers, redacted previews, stack-hash persistence proof, and abuse/provider-forwarding proof; distributed rate-limit provider execution, live Postgres tenant-isolation fixtures, provider forwarding replay/no-PII proof, and CI remain gated under `GAP-081`.

Dashboard error-report read/write responses now suppress tenant, ErrorReport, audit, raw metadata, stack hash, and internal persistence identifiers (`tenantIdEchoed=false`, `errorReportIdEchoed=false`, `errorReportIdsEchoed=false`, `auditIdEchoed=false`, `rawMetadataEchoed=false`, `stackHashEchoed=false`, `internalPersistenceIdsEchoed=false`) on DB and local-fallback paths while preserving redacted message/metadata, stack-hash persistence proof, alert route, and provider escalation blockers; live Sentry/OTel/provider alert evidence and CI remain gated under `GAP-011`/`GAP-081`.

Public privacy-request receipts now suppress tenant, PrivacyRequest, AuditLog, idempotency, requester hash, and raw payload identifiers (`tenantIdEchoed=false`, `privacyRequestIdEchoed=false`, `auditIdEchoed=false`, `internalPersistenceIdsEchoed=false`) on DB and local fallback paths while preserving tenant slug, safe receipt policy fields, and identity/relationship proof requirements; identity proofing, legal workers, object-storage export/delete, tenant-isolated integration tests, and CI remain gated under `GAP-098`.

Public secure-upload intent receipts now suppress tenant, FileAsset, SignedUrlGrant, ReferenceImage, AuditLog, bucket, object-key, upload URL field, signed URL field, signed URL hash, raw plan, and local draft echoes (`tenantIdEchoed=false`, `fileAssetIdEchoed=false`, `signedUrlGrantIdEchoed=false`, `referenceImageIdEchoed=false`, `auditIdEchoed=false`, `uploadUrlEchoed=false`, `signedUploadUrlEchoed=false`, `internalPersistenceIdsEchoed=false`) on DB and local fallback paths while preserving tenant slug, validation, provider-gated upload status, and required next work; provider-signed URL minting, byte verification, malware scanning, private bucket ACL proof, tenant-isolated storage/provider tests, and CI remain gated under `GAP-005`/`GAP-033`/`GAP-096`/`GAP-097`.

Reference upload provider artifact review now redacts provider-signed upload/download URLs, bucket and object keys, signed URL hashes, FileAsset/SignedUrlGrant/ReferenceImage/AuditLog selectors, byte and magic-byte verification transcripts, malware scanner payloads, quarantine metadata, private ACL and anonymous/cross-tenant fetch-denial proof, transactional persistence payloads, local upload-policy preview output, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/booking/reference labels, contacts, credentials, tokens, and authorization headers before retained reference-upload evidence is accepted. This remains a local/static artifact-safety contract only; provider-signed URL minting, byte verification, magic-byte validation, malware scan/quarantine, transactional replay/isolation proof, private ACL enforcement, anonymous/cross-tenant fetch-denial tests, fresh CI proof, and redacted artifact capture remain gated under `GAP-033`.

Provider storage/upload artifact review now redacts provider signed URL transcripts, bucket/object keys, FileAsset/SignedUrlGrant/ReferenceImage/PortfolioImage/AuditLog selectors, malware scan and quarantine payloads, metadata/derivative traces, private ACL and anonymous/cross-tenant denial proof, transactional persistence payloads, command output, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/file labels, contacts, credentials, tokens, and authorization headers before retained storage evidence is accepted. This remains a local/static artifact-safety contract only; live provider selection/config/secrets, signed upload/download URL minting, FileAsset/link/audit/grant persistence proof, scan/derivative worker execution, private-original denial, approved-derivative public-read proof, tenant isolation, CI proof, and provider-backed `ProviderStorageUploadRun` persistence remain gated under `GAP-005`.

Public web launch artifact review now redacts rendered HTML, JSON-LD/canonical/sitemap/robots snapshots, route request/response bodies, local/static-demo fallback markers, public booking/contact/error-report payloads, media derivative and placeholder-asset evidence, Playwright/axe/Lighthouse traces, screenshots/videos, command output, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/customer/client/booking/media labels, contacts, credentials, tokens, and authorization headers before retained launch evidence is accepted. This remains a local/static artifact-safety contract only; web typecheck/build/test/smoke, Playwright, axe/Lighthouse, provider-backed public routes, real media derivatives, runtime SEO validation, legal review, CI evidence, and provider-backed `PublicWebLaunchRun` persistence remain gated under `GAP-006`.

Upload scan worker artifact review now redacts tenant, FileAsset, scan-job, malware-scan, audit, bucket, checksum/hash, source/quarantine/derivative object-key, signed URL, scanner/provider payload, credential, contact, and stack fields before retained scanner/storage evidence is accepted. This remains a local/static artifact-safety contract only; live object-storage byte inspection, malware scanner provider execution, quarantine/rejection proof, provider-backed FileAsset persistence, and private-original/public-derivative integration tests remain gated under `GAP-096`.

Private storage signed URL artifact review now redacts tenant, FileAsset, SignedUrlGrant, ReferenceImage, audit, revocation, revoked-user, bucket, object-key/source-object-key, signed URL, signed URL hash, provider payload, credential, contact, and stack fields before retained signed-storage evidence is accepted. This remains a local/static artifact-safety contract only; live S3/Supabase signing, private bucket ACL denial, provider-backed grant persistence, tenant-scoped access proof, and approved derivative public-read tests remain gated under `GAP-097`.

Retention enforcement artifact review now redacts dry-run/tombstone record selectors (`recordId`, `sourceRecordId`) in addition to worker run IDs, dry-run fingerprints, storage object keys, export artifacts, rollback notes, audit-log IDs, contact fields, stack traces, and credentials before any secret-safe artifact is retained; production retention workers, Postgres/object-storage execution, restore replay, tenant-isolation tests, and CI remain gated under `GAP-099`.

Public deposit-session local previews and DB-backed deposit draft receipts now return allowlisted stored-session/checkout receipts with raw tenant, booking, client-reference, local deposit session, checkout URL field, mock checkout URL, and mock provider session identifiers suppressed (`tenantIdEchoed=false`, `bookingRequestIdEchoed=false`, `clientReferenceIdEchoed=false`, `localDepositSessionIdEchoed=false`, `checkoutUrlEchoed=false`, `providerSessionIdEchoed=false`, `mockCheckoutUrlEchoed=false`, `rawProviderSessionIdEchoed=false`, `internalPersistenceIdsEchoed=false`) while preserving policy, amount/currency/status, and Stripe readiness blockers; live Stripe Checkout execution, webhook reconciliation, tenant-isolated payment tests, and CI remain gated under `GAP-004`/`GAP-049`/`GAP-050`.

Public contact and waitlist receipts now suppress tenant identifiers (`tenantIdEchoed=false`) on DB and local fallback paths while preserving tenant slug, redacted contact/waitlist status, idempotency/replay proof, and provider-handoff boundaries; provider delivery, replay/suppression proof, tenant-isolated integration tests, and CI remain gated under `GAP-010`/`GAP-026`/`GAP-029`/`GAP-064`.

Booking/contact runtime artifact review now redacts public contact route request/response payloads, contact/message bodies, destination hashes, idempotency keys and replay proof, Client/MessageThread/NotificationDelivery/NotificationProviderHandoff/AuditLog selectors, local fallback audit metadata, provider handoff payloads, no-live-payment/deposit boundaries, confirmation workflow artifacts, browser/API E2E traces/screenshots/videos, command output, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/client/booking/contact/payment/medical labels, contacts, credentials, tokens, and authorization headers before retained booking/contact evidence is accepted. This remains a local/static artifact-safety contract only; seeded DB transaction proof, confirmation workflow reads, provider-backed `BookingContactRun` persistence, tenant-isolation integration, browser/API E2E, provider sandbox handoff evidence, web typecheck/build, CI proof, and secret-safe artifact review remain gated under `GAP-029`.

Public preference-center and one-click unsubscribe DB and non-production local-contract receipts now suppress tenant identifiers (`tenantIdEchoed=false`) alongside client, preference, suppression, idempotency, audit, raw token, token hash, and write-payload no-echo proof while preserving tenant slug and safe plan/contract metadata; provider List-Unsubscribe execution, suppression reconciliation, replay evidence, tenant-isolated integration tests, and CI remain gated under `GAP-010`/`GAP-061`/`GAP-067`/`GAP-069`.

Phase 9 app runtime/build artifact review now redacts route URLs, rendered HTML/DOM, command/build output, CI artifact URLs, run/commit selectors, tenant/client/booking/deposit/appointment/message identifiers, message bodies, stack traces, provider payloads, Expo/device/push tokens, credentials, email, and phone values before retained runtime/build evidence is accepted. This remains a local/static artifact-safety contract only; actual web/dashboard/mobile builds, Playwright/browser/device smokes, booking-to-notification runtime proof with provider sends disabled, CI artifact capture, and provider-disabled evidence remain gated under `GAP-070`.

Notification automation artifact review now redacts route URLs, rendered HTML/DOM, command output, CI artifact URLs, tenant/client/booking/deposit/message/thread/template/queue/delivery/webhook identifiers, lifecycle message bodies, provider payloads, Expo/device/push/SMS/email tokens, credentials, contacts, and stack traces before retained notification automation evidence is accepted. This remains a local/static artifact-safety contract only; queue integration, dashboard Playwright, mobile/device QA, provider sandbox sends, preference/STOP persistence, booking/deposit/aftercare/travel E2E, CI artifact capture, and secret-safe artifact production remain gated under `GAP-069`.

SEO analytics attribution artifact review now redacts tenant, booking, portfolio, attribution, campaign, AnalyticsEvent, Search Console imported-row/operation-run/query/page, UTM/referrer URL, rendered dashboard/report, command/log, CI artifact, stack, provider payload, credential, contact, medical, payment, and private fields before retained attribution evidence is accepted. This remains a local/static artifact-safety contract only; provider-backed analytics persistence, seeded dashboard report execution, Search Console import jobs, Playwright click-through attribution, booking attribution integration, CI evidence, and secret-safe artifact production remain gated under `GAP-074`.

CI coverage reporting artifact packets now redact run/commit/branch-protection selectors, artifact URLs, trace/screenshot/video/debug paths, command output, build logs, raw payloads, environment/database/DSN fields, stacks, credentials, contacts, tokens, and authorization headers before retained CI evidence is accepted. This remains a local/static artifact-safety contract only; live GitHub Actions run proof, uploaded coverage/report artifacts, branch protection settings proof, flaky-policy/debug-media proof, provider-backed `CiCoverageRun` persistence, and final redacted packet capture remain gated under `GAP-111`.

Database integration runtime artifact review now redacts database/direct URLs, Postgres/Prisma/env/DSN fields, migration/seed/verify command output, tenant/audit/workflow/row selectors, destructive-reset and rollback notes, CI run selectors, redacted transcript paths, raw payloads, stacks/errors, credentials, tokens, and authorization headers before retained DB evidence is accepted. This remains a local/static artifact-safety contract only; non-production Postgres provisioning, Prisma lifecycle execution, tenant-isolation/workflow/audit-log integration tests, CI DB artifacts, and provider-backed `DbIntegrationRun` persistence remain gated under `GAP-107`.

Seed runtime execution artifact review now redacts database URLs, seed command transcripts, fake/demo data reports, legal-placeholder proof, production-provider ban output, seeded tenant/member/workflow/payment/file/message/SEO/release/flag/audit row selectors, Prisma migration/seed/query output, web/API and dashboard seeded-data smoke payloads, CI/clean-checkout selectors, artifact paths, provider/database fields, tenant/user/client labels, contacts, credentials, tokens, and authorization headers before retained seed evidence is accepted. This remains a local/static artifact-safety contract only; non-production Postgres provisioning, DATABASE_URL configuration, Prisma validate/generate/migrate/seed execution, seeded-domain query proof, app smokes, CI or clean-checkout evidence, redacted transcript capture, and provider-backed `SeedRuntimeExecutionRun` persistence remain gated under `GAP-018`.

Mobile testing execution artifact review now redacts Expo/EAS/provider tokens, device/simulator/emulator identifiers, push/crash/API/offline/auth/session/tenant/client evidence, EAS build/update/rollback URLs and transcripts, screenshots/videos/traces/artifact paths, accessibility/biometric/native logs, env/database/DSN fields, CI run selectors, commit/run IDs, raw payloads, stacks/errors, credentials, contacts, and authorization headers before retained mobile testing evidence is accepted. This remains a local/static artifact-safety contract only; Expo runtime startup, iOS/Android simulator/emulator smoke, physical-device QA, provider-backed push/crash/API/OTA proof, EAS preview/update rollback, CI mobile checks, and provider-backed `MobileTestingRun` persistence remain gated under `GAP-108`.

Mobile launch artifact review now redacts Expo/EAS/provider tokens, device/simulator/emulator identifiers, session/auth/tenant/client evidence, API route transcripts, offline/crash/raw payloads, EAS build/update selectors, OTA rollback logs, push evidence, screenshots/videos/traces/artifact paths, accessibility/biometric/native logs, command output, CI/workflow/run selectors, database/DSN fields, credentials, contacts, and authorization headers before retained launch evidence is accepted. This remains a local/static artifact-safety contract only; Expo runtime, simulator/device QA, EAS preview/update rollback, provider QA, CI mobile evidence, and provider-backed `MobileLaunchRun` persistence remain gated under `GAP-008`.

Mobile deployment runtime artifact review now redacts EAS/Expo/Sentry/store/provider identifiers, App Store/Play Store submission labels, bundle/package names, native signing/provisioning/keystore and push credentials, build/install/OTA/CI URLs, runtime channels/profiles/build IDs, update and rollback transcripts, screenshots/videos/traces/artifact paths, device QA/crash/accessibility/native logs, env/database/DSN fields, raw payloads, tenant/user/run/commit IDs, contacts, tokens, and authorization headers before retained deployment evidence is accepted. This remains a local/static artifact-safety contract only; approved EAS builds, native credential proof, store readiness, device QA, crash/push/OTA execution, CI mobile deployment artifacts, and provider-backed `MobileDeploymentRun` persistence remain gated under `GAP-116`.

Provider environment runtime artifact review now redacts provider/project/resource identifiers, preview/staging/production/deploy URLs, strict-env and verifier output, provider smoke transcripts, database migration dry-run output, storage ACL proof, source-map proof, GitHub protection notes, secret-store destinations, raw handoff payloads, artifact labels/paths, env/database/DSN fields, CI/workflow/run/commit selectors, tenant/user/run IDs, contacts, credentials, tokens, and authorization headers before retained provider-environment evidence is accepted. This remains a local/static artifact-safety contract only; preview/staging/production provisioning, provider smoke checks, database dry-runs, storage ACL smoke, EAS preview build, source-map smoke, GitHub environment protection audit, secret-store proof, CI artifacts, redacted handoff packet capture, and provider-backed `ProviderEnvironmentRun` persistence remain gated under `GAP-114`.

Secret management runtime artifact review now redacts secret values, provider secret-store destinations/references, strict-env/verifier output, committed-secret scan findings/diffs, masked CI logs, provider audit labels/references, rotation cadence and dual-control notes, incident/tabletop transcripts, raw audit payloads, artifact paths, env/database/DSN fields, CI/workflow/run/commit selectors, tenant/user/run IDs, stacks/errors, contacts, credentials, tokens, and authorization headers before retained secret-management evidence is accepted. This remains a local/static artifact-safety contract only; provider secret-store configuration, real secret-backed strict env checks, masked CI log capture, provider audit-log proof, rotation/dual-control approval evidence, incident tabletop proof, committed-secret scan execution, CI artifacts, and provider-backed `SecretManagementRun` persistence remain gated under `GAP-115`.

Provider contract runtime artifact review now redacts provider request/response transcripts, raw bodies/payloads, webhook signatures, OAuth/session/rate-limit/idempotency/replay proof, storage/calendar/payment/messaging provider payloads, sandbox logs, provider URLs, Redis/Postgres DSNs, stacks/errors/output, env/database fields, CI/workflow/run selectors, commit/run IDs, fixture/event labels, contacts, credentials, tokens, and authorization headers before retained provider-contract evidence is accepted. This remains a local/static artifact-safety contract only; Stripe CLI, Google Calendar, storage, email/SMS/Expo/Sentry, auth/session, distributed rate-limit sandbox proof, sanitized fixture capture, CI artifacts, and provider-backed `ProviderContractRun` persistence remain gated under `GAP-110`.

Provider session runtime artifact review now redacts provider IDs, login/logout/session callback payloads, cookies, CSRF values, session-store rows, TenantMember and CustomRole selectors, revocation lookups, mobile/device token evidence, auth audit rows, tenant-isolation/denial transcripts, command output, CI/workflow/run selectors, artifact paths, provider/database fields and DSNs, tenant/user/account labels, contacts, credentials, tokens, and authorization headers before retained provider-session evidence is accepted. This remains a local/static artifact-safety contract only; selected auth provider configuration, live callbacks, persisted session/revocation/audit proof, tenant-isolation smoke tests, command evidence, CI proof, redacted bundle capture, and provider-backed `ProviderSessionRun` persistence remain gated under `GAP-003`.

Legal review runtime artifact review now redacts counsel/attorney/privileged labels, approval/reviewer metadata, legal document/version selectors, placeholder/reviewed copy traces, consent/SMS/aftercare/refund policy text, route/report/document bodies, legal audit and quality-gate output, command output, CI/workflow/run selectors, artifact paths, stacks/errors/logs, provider/database/persistence fields, tenant/user/client labels, contacts, credentials, tokens, and authorization headers before retained legal-review evidence is accepted. This remains a local/static artifact-safety contract only; qualified counsel approval, reviewed-copy replacement, legal audit execution, CI legal evidence, and provider-backed `LegalReviewRun` persistence remain gated under `GAP-013`.

Dashboard launch artifact review now redacts provider auth/session payloads, seeded tenant data, tenant-scoped route/read payloads, mutation and AuditLog payloads, RBAC/cross-tenant denial transcripts, rendered HTML/DOM, Playwright traces/screenshots/videos, loading/empty/error-state artifacts, build/typecheck/test output, CI/workflow/run selectors, artifact paths, provider/database fields and DSNs, tenant/user/client/customer/payment/medical labels, contacts, credentials, tokens, and authorization headers before retained dashboard launch evidence is accepted. This remains a local/static artifact-safety contract only; live dashboard typecheck/build/test, Playwright smoke, seeded tenant data, provider auth, provider-backed mutations/audit persistence, RBAC/cross-tenant denial proof, CI proof, secret-safe artifact capture, and provider-backed `DashboardLaunchRun` persistence remain gated under `GAP-007`.

Performance/load runtime artifact review now redacts Lighthouse/Core Web Vitals traces, screenshots/videos/artifact paths, route URLs, command output, raw request/response bodies, load-test/webhook/upload provider payloads, DB EXPLAIN/ANALYZE output, image benchmark paths, regression triage notes, env/database/DSN fields, CI/workflow/run/commit selectors, route/load/query labels, tenant/user/booking IDs, contacts, credentials, tokens, and authorization headers before retained performance evidence is accepted. This remains a local/static artifact-safety contract only; live Lighthouse/load/database/image benchmark execution, CI performance artifacts, regression triage, redacted packet capture, and provider-backed `PerformanceLoadRun` persistence remain gated under `GAP-112`.

Testing launch execution artifact review now redacts command output, JUnit/JSON/report bodies, coverage/failure-debug artifacts, traces/screenshots/videos, branch-protection and flaky-policy notes, raw provider/mobile outputs, stack/error logs, env/database/DSN fields, CI/workflow/commit/run selectors, test/report/artifact labels, tenant/user/client IDs, PII, medical/payment fields, credentials, tokens, and authorization headers before retained testing evidence is accepted. This remains a local/static artifact-safety contract only; frozen install, typecheck, unit/coverage/E2E/build/database/provider/security/mobile execution, CI quality run, branch protection, flaky policy, failure-debug evidence, secret-safe artifact proof, and provider-backed `TestingLaunchExecutionRun` persistence remain gated under `GAP-012`.

Phase 14 runner artifact review now redacts install/typecheck/unit/E2E command output, manifest verifier output, Playwright traces/screenshots/videos, coverage/JUnit/JSON reports, triage/scaffold/flaky-policy artifacts, branch-protection notes, raw failure payloads, env/database/DSN fields, stacks/errors, CI/workflow/commit/run selectors, report/artifact labels, contacts, credentials, tokens, and authorization headers before retained runner evidence is accepted. This remains a local/static artifact-safety contract only; frozen install, typecheck/unit runner execution, Playwright browser install, E2E proof, CI workflow proof, failure triage/fixes, flaky policy, and provider-backed `Phase14RunnerRun` persistence remain gated under `GAP-105`.

Runtime evidence matrix artifact review now redacts command output, raw logs/transcripts, workspace/handoff/quality/typecheck/unit/build evidence, production blocker notes, artifact paths, traces/screenshots/videos, CI/workflow/commit/run selectors, runtime/evidence/workspace labels, persistence/database fields and DSNs, tenant/user/account/customer labels, stacks/errors, contacts, credentials, tokens, and authorization headers before retained runtime readiness evidence is accepted. This remains a local/static artifact-safety contract only; workspace/runtime/handoff/quality commands, typecheck/unit/build proof, CI runtime readiness, production-blocker visibility, redacted bundle capture, and provider-backed `RuntimeEvidenceRun` persistence remain gated under `GAP-132`.

Documentation audit runtime artifact review now redacts markdown/link/inventory/consistency audit outputs, raw document excerpts, quality/docs command logs, CI/workflow/commit/run selectors, document/audit/status labels, provider review payloads, stale-provider status proof, legal reviewer and privileged-review notes, artifact paths, provider/resource/database fields, tenant/user labels, database DSNs, stacks/errors, contacts, credentials, tokens, and authorization headers before retained documentation-audit evidence is accepted. This remains a local/static artifact-safety contract only; live quality docs execution, CI evidence, provider review, legal review, stale-provider proof, redacted bundle capture, and provider-backed `DocumentationAuditRun` persistence remain gated under `GAP-124`.

Agent task tracking artifact review now redacts GitHub issue/project/tracking URLs, queue task payloads, handoff-doc links, GAP tracker links, status traces, queue/status/tracking labels, ledger/audit/sync logs, CI/workflow/commit/run selectors, assignee/actor/reviewer/provider labels, raw GitHub payloads, artifact paths, provider/database fields, tenant/user labels, database DSNs, stacks/errors, contacts, credentials, tokens, and authorization headers before retained task-tracking evidence is accepted. This remains a local/static artifact-safety contract only; live GitHub issue creation, Project sync, traceable status updates, CI artifacts, redacted bundle capture, and provider-backed `AgentTaskTrackingRun` persistence remain gated under `GAP-123`.

Required-checks runtime artifact review now redacts GitHub/repository settings, branch-protection audit transcripts, required-check and CI logs, merge-block proof, CODEOWNERS review bodies, raw PR payloads, PR/diff artifacts, artifact paths, CI/workflow/commit/run selectors, GitHub/PR/branch/check labels, tenant/user/account labels, stacks/errors, contacts, credentials, tokens, and authorization headers before retained required-check evidence is accepted. This remains a local/static artifact-safety contract only; required-check command execution, CI quality job proof, branch-protection and repository-settings audits, failing-PR merge-block proof, CODEOWNERS review proof, redacted bundle capture, and provider-backed `RequiredChecksRun` persistence remain gated under `GAP-129`.

Repository governance artifact review now redacts GitHub/repository settings, branch-protection and required-check payloads, CODEOWNERS/reviewer details, security/dependabot/secret-scanning alert proof, merge-rule and bypass evidence, enforcement PR/check-run URLs, raw repository API payloads, command output/logs/transcripts, PR/diff artifacts, artifact paths, CI/workflow/commit/run selectors, repository/governance/security/check labels, tenant/user/account labels, stacks/errors, contacts, credentials, tokens, and authorization headers before retained repository-governance evidence is accepted. This remains a local/static artifact-safety contract only; live GitHub branch protection, required-check, CODEOWNERS review, secret-scanning, security-alert, merge-rule, enforcement-test PR, CI, redacted bundle capture, and provider-backed `RepositoryGovernanceRun` persistence remain gated under `GAP-125`.

Workspace required-checks artifact review now redacts workspace audit output, GitHub/repository settings, branch-protection transcripts, CI logs, failing workspace PR merge-block proof, PR GAP diff payloads, CODEOWNERS review bodies, raw GitHub/PR payloads, diff artifacts, CI/workflow/commit/run selectors, workspace/GAP-diff/check labels, tenant/user/account labels, stacks/errors, contacts, credentials, tokens, and authorization headers before retained workspace required-check evidence is accepted. This remains a local/static artifact-safety contract only; workspace required-check commands, workspace/quality gates, CI quality proof, branch protection review, failing workspace PR and GAP-diff merge-block proof, redacted log review, redacted bundle capture, and provider-backed `WorkspaceRequiredChecksRun` persistence remain gated under `GAP-133`.

Workspace runtime toolchain artifact review now redacts dependency install logs, package-script/import audit output, workspace/toolchain command output, web/dashboard build logs, runtime-readiness and production-blocker reports, artifact paths, manifest/test/typecheck/quality evidence, CI/workflow/commit/run selectors, workspace/toolchain/build labels, persistence/database fields and DSNs, tenant/user/account labels, stacks/errors, contacts, credentials, tokens, and authorization headers before retained workspace runtime evidence is accepted. This remains a local/static artifact-safety contract only; package typecheck/test, workspace toolchain/all commands, dependency install, web/dashboard builds, CI workspace runtime job, production-blocker visibility, redacted bundle capture, and provider-backed `WorkspaceRuntimeToolchainRun` persistence remain gated under `GAP-130`.

Dependency install artifact review now redacts install/frozen-lockfile output, lockfile and package/dependency excerpts, workspace audit/typecheck/lint/unit command output, production-blocker manifests, CI/workflow/commit/run selectors, artifact paths, package-manager/cache metadata, provider/database fields and DSNs, tenant/user/account labels, stacks/errors/logs, contacts, credentials, tokens, and authorization headers before retained install evidence is accepted. This remains a local/static artifact-safety contract only; live install, frozen-lockfile install, workspace/typecheck/lint/unit execution, CI quality proof, production-blocker visibility, redacted bundle capture, and provider-backed `DependencyInstallRun` persistence remain gated under `GAP-001`.

Prisma lifecycle artifact review now redacts database/direct URLs, connection strings, generated SQL, schema/client generation output, migration and drift payloads, seed execution manifests, destructive reset and rollback reports, production database guard evidence, command output/transcripts, CI/workflow/commit/run selectors, artifact paths, provider/database fields, tenant/user/account/row labels, migration/SQL/guard labels, stacks/errors/logs, contacts, credentials, tokens, and authorization headers before retained Prisma lifecycle evidence is accepted. This remains a local/static artifact-safety contract only; live non-production Postgres provisioning, Prisma validate/generate/migrate/seed execution, SQL review, drift proof, production URL guard proof, CI DB lifecycle proof, redacted bundle capture, and provider-backed `PrismaLifecycleRun` persistence remain gated under `GAP-002`.

Quality gate runtime artifact review now redacts quality/gap/governance/required-check command output, manifest/audit/fixture reports, gate summaries, production blocker notes, CI/workflow/commit/run selectors, report/artifact paths, provider/persistence/database fields and DSNs, tenant/user/account labels, stacks/errors, contacts, credentials, tokens, and authorization headers before retained quality evidence is accepted. This remains a local/static artifact-safety contract only; package typecheck/test, quality docs/gaps/governance/required-check gates, `quality:all`, CI quality job, artifact capture, and provider-backed `QualityGateRun` persistence remain gated under `GAP-126`.

Legal review runtime artifact review now redacts counsel/attorney/privileged labels, approval/reviewer metadata, placeholder and reviewed legal copy, consent/SMS/aftercare/refund policy text, legal audit and quality-gate output, CI/run/artifact selectors, route/report/document bodies, stacks/errors/logs, provider/database/persistence fields, tenant/user/client identifiers, contacts, credentials, tokens, and authorization headers before retained legal evidence is accepted. This remains a local/static artifact-safety contract only; qualified counsel approval, reviewed-copy replacement, legal audit execution, CI legal evidence, and provider-backed `LegalReviewRun` persistence remain gated under `GAP-013`.

## Phase 2 domain event/audit runtime

Status: **Local structural transaction contract with provider-backed persistence still evidence-gated**.

`executeDomainEventAuditLifecycleTransaction` keeps booking-state, payment-audit, and audit-log persistence in the tenant-scoped idempotency transaction contract. Durable metadata now stores idempotency proof flags (`idempotencyPersisted=true`, `rawIdempotencyKeyStored=false`, `internalPersistenceIdsStored=false`) instead of duplicating raw idempotency keys or internal row IDs. Provider-backed transaction execution, replay proof, rollback proof, tenant-isolated integration tests, CI artifacts, and secret-safe database evidence remain required before GAP-024 can close.

## Phase 15 deployment/readiness routes

### `GET /api/deployment/readiness`

**Status:** Auth-guarded and tenant-scoped readiness endpoint with DB fallback behavior.

Returns a deployment readiness preview built from `@inkroute/deployment` helpers:

- environment readiness summary
- deployment plan
- launch checklist
- handoff tasks
- production blocked marker

The route must not expose secret values, tenant IDs, raw request IDs, unverified workflow-run IDs/URLs, or AuditLog/internal persistence IDs. DB-backed reads and writes return proof flags only (`tenantIdEchoed=false`, `auditIdEchoed=false`, `internalPersistenceIdsEchoed=false`, `workflowRunIdEchoed=false`, `workflowRunUrlEchoed=false`, plus `rawRequestIdStored=false`, `rawRequestIdEchoed=false`, `workflowRunIdVerified=false`, and `workflowRunLinked=false` on deployment-readiness writes); production promotion still requires dashboard auth, tenant/admin RBAC, rate limiting, provider/runtime evidence, CI/CD environment proof, and secret-redaction artifacts.

CI/CD deployment automation artifact review now redacts workflow/run/deployment selectors, repository identifiers, deployment URLs, commit SHAs, tenant/release/audit/idempotency selectors, provider payloads, database URLs, credentials, service-account material, and contact values before retained automation evidence is accepted. This remains a local/static artifact-safety contract only; protected GitHub environments, live workflow dispatch, Vercel/EAS/Sentry/Search Console execution, ReleaseRecord CI-result writes, and CI artifacts remain gated under `GAP-089`.

Deployment launch evidence artifact review now redacts Vercel/GitHub/EAS/Expo/Sentry/Supabase/Neon provider fields, protected-environment approval payloads, provider-env verifier output, preview/prod smoke transcripts, strict-env and secret evidence, database migration dry-run and backup/restore logs, storage evidence, native credential and OTA rollback evidence, CI deployment-gate output, Sentry release/source-map upload logs, deployment rollback drill packets, launch evidence packet paths, workflow/run/deployment selectors, provider/database fields, tenant/user labels, stacks/errors/logs, contacts, credentials, tokens, and authorization headers before retained deployment launch evidence is accepted. This remains a local/static artifact-safety contract only; live Vercel/GitHub environment setup, secret-backed provider evidence, preview/prod dry runs, database/storage operations, EAS/native credentials/OTA rollback, CI deployment gate, Sentry release upload, rollback drill, launch evidence packet, provider-backed `DeploymentLaunchEvidenceRun` persistence, and secret-safe provider artifact proof remain gated under `GAP-014`.

Production launch evidence artifact review now redacts launch verifier output, checklist blocker reports, CI/build/test evidence, database operation bundles, provider/secret readiness bundles, security/privacy/trust evidence, accessibility/SEO/performance artifacts, mobile release evidence, legal and explicit approval payloads, rollback/operations transcripts, workflow/run/deployment selectors, production URLs, artifact paths, provider/database fields, tenant/user labels, stacks/errors/logs, contacts, credentials, tokens, and authorization headers before retained launch evidence is accepted. This remains a local/static artifact-safety contract only; live launch bundle verification, legal approval, rollback and operations proof, explicit production approval, CI artifact proof, redacted bundle capture, and provider-backed `ProductionLaunchEvidenceRun` persistence remain gated under `GAP-118`.

Launch operations artifact review now redacts primary/backup owner contacts, on-call schedules, staffed coverage evidence, alert routing provider webhooks, incident/privacy/support/rollback drill transcripts, raw support/customer data, monitoring dashboard URLs, communications template approvals, explicit operations approval payloads, CI/workflow/run selectors, artifact paths, provider/database fields, tenant/user labels, stacks/errors/logs, contacts, credentials, tokens, and authorization headers before retained launch-operations evidence is accepted. This remains a local/static artifact-safety contract only; live staffed owner coverage, alert routing, incident/privacy/support/rollback drills, monitoring, communications approval, explicit operations approval, CI artifact proof, redacted bundle capture, and provider-backed `LaunchOperationsRun` persistence remain gated under `GAP-120`.

Handoff tooling artifact review now redacts dependency install output, package typecheck/test output, verify-docs/audit/next/ledger/tooling/task-sync reports, handoff:all logs, missing-script details, queue/ledger parity output, report artifact paths, CI/workflow/run selectors, provider URLs, raw logs/output, environment values, provider/database fields, tenant/user/actor labels, stacks/errors, contacts, credentials, tokens, and authorization headers before retained handoff-tooling evidence is accepted. This remains a local/static artifact-safety contract only; live dependency install, package typecheck/test, handoff script execution, CI run, report capture, redacted bundle capture, and provider-backed `HandoffToolingRun` persistence remain gated under `GAP-121`.

PR gap evidence enforcement artifact review now redacts fixture diffs, GAP audit output, quality:all output, branch-protection and repository-settings payloads, required-check/check-run logs, failing/passing PR proof bodies, live merge-block evidence, secret-safe review notes, command output/transcripts, PR URLs, CI/workflow/run/commit selectors, PR/branch/merge-block/check labels, artifact paths, provider/database fields, tenant/user/actor labels, stacks/errors/logs, contacts, credentials, tokens, and authorization headers before retained PR-gap enforcement evidence is accepted. This remains a local/static artifact-safety contract only; live branch protection, durable persistence row, secret-safe logs, failing PR merge-block, passing PR evidence, command/artifact proof, redacted bundle capture, and provider-backed `PrGapEvidenceEnforcementRun` persistence remain gated under `GAP-122`.

PR diff evidence artifact review now redacts fixture diff bodies, GAP audit and tracker-diff output, no-PR skip simulations, pull_request CI payloads, missing merge-base fallback logs, positive/negative fixture artifact paths, secret-safe review notes, command output/transcripts, PR/repository URLs, branch names, PR/GAP-diff/merge-base labels, CI/workflow/run/commit selectors, provider/database fields, tenant/user/account labels, stacks/errors/logs, contacts, credentials, tokens, and authorization headers before retained PR-diff evidence is accepted. This remains a local/static artifact-safety contract only; live persisted row evidence, command/artifact proof, secret-safe log review, redacted bundle capture, CI pull_request proof, and branch-level protection enforcement remain gated under `GAP-127`.

Semantic documentation artifact review now redacts raw markdown/document excerpts, claim logs, documentation inventory and consistency audit payloads, quality-docs output, link/path audit data, CI/workflow/run selectors, artifact paths, provider/runtime/legal boundary reports, counsel/reviewer approval details, provider/database fields, tenant/user/client labels, stacks/errors/logs, contacts, credentials, tokens, and authorization headers before retained semantic-documentation evidence is accepted. This remains a local/static artifact-safety contract only; live quality-docs CI proof, persisted `SemanticDocumentationRun` rows, command/artifact proof, legal/provider boundary evidence, and redacted bundle capture remain gated under `GAP-128`.

Static dependency audit artifact review now redacts dependency install output, workspace import/typecheck/build output, dependency trees, lockfile and manifest excerpts, peer/version review payloads, runtime-resolution proof, CI/workflow/run selectors, artifact paths, registry/repository/package labels, provider/database fields, tenant/user/account labels, stacks/errors/logs, contacts, credentials, tokens, and authorization headers before retained dependency-audit evidence is accepted. This remains a local/static artifact-safety contract only; live install/typecheck/build execution, CI workspace-resolution proof, peer/version review, runtime dependency proof, persisted `StaticDependencyAuditRun` rows, and redacted bundle capture remain gated under `GAP-131`.

Agent execution ledger artifact review now redacts external agent transcripts, command stdout/stderr, diffs/patches, changed-files matrices, diff/patch artifact paths, provider-evidence labels and URLs, remaining-gap summaries, secret-safety reviews, external-result import metadata, GAP tracker update payloads, agent/task/result labels, queue/ledger parity logs, workflow/CI/commit selectors, artifact paths, provider/database fields, tenant/user/actor labels, stacks/errors/logs, contacts, credentials, tokens, database DSNs, and authorization headers before retained agent-ledger evidence is accepted. This remains a local/static artifact-safety contract only; live external agent result import, redacted transcripts, secret-safety review, changed-files matrix, GAP tracker evidence updates, CI artifact proof, redacted bundle capture, and provider-backed `AgentExecutionLedgerRun` persistence remain gated under `GAP-119`.

### `POST /api/deployment/readiness`

**Status:** Request validation and audit-ready persistence behavior is in place; deployment execution (migration jobs, provider publishes, approvals, rollbacks) is intentionally out-of-band until CI/CD environments and credentials are provisioned.
